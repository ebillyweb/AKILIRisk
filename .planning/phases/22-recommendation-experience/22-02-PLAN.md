---
phase: 22-recommendation-experience
plan: "02"
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/lib/recommendations/solution-lifecycle.ts
  - src/lib/recommendations/guidance-package.ts
  - src/lib/recommendations/compose-solution.ts
  - src/lib/recommendations/guidance-package.test.ts
  - src/lib/recommendations/solution-lifecycle.test.ts
autonomous: true
requirements: [LIFECYCLE-01]

must_haves:
  truths:
    - "Lifecycle state machine supports GENERATED->REVIEWED->INCLUDED->IN_PROGRESS->COMPLETED and DEFERRED transitions"
    - "Guidance package aggregates recommendations across all completed assessments for a single client"
    - "Cross-assessment deduplication keeps highest urgency and merges evidence arrays"
    - "Existing PENDING/ACCEPTED transitions continue to work for backward compatibility"
  artifacts:
    - path: "src/lib/recommendations/solution-lifecycle.ts"
      provides: "Extended state machine with new transitions and DEFERRED/INCLUDED handling"
      exports: ["transitionRecommendationStatus", "SOLUTION_ACTIONS"]
    - path: "src/lib/recommendations/guidance-package.ts"
      provides: "Per-client guidance package aggregation with deduplication"
      exports: ["getGuidancePackageForClient"]
    - path: "src/lib/recommendations/compose-solution.ts"
      provides: "Extended composition with override policy awareness"
      exports: ["composeSolution"]
  key_links:
    - from: "src/lib/recommendations/guidance-package.ts"
      to: "src/lib/recommendations/compose-solution.ts"
      via: "composeSolution call per recommendation"
      pattern: "composeSolution"
    - from: "src/lib/recommendations/guidance-package.ts"
      to: "prisma.assessmentRecommendation.findMany"
      via: "cross-assessment query"
      pattern: "assessmentRecommendation\\.findMany"
---

<objective>
Extend the lifecycle state machine for the implementation-focused flow (D-07, D-08) and build the per-client guidance package aggregation engine (D-01, D-02) that deduplicates recommendations across assessments.

Purpose: The guidance package is the central data structure consumed by all three role UIs. Without it, advisors cannot review holistic client guidance, and clients cannot see a unified Strategic Action Plan.

Output: Extended lifecycle state machine supporting new transitions, per-client guidance package query with dedup, extended composition with override policy integration.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recommendation-experience/22-CONTEXT.md
@.planning/phases/22-recommendation-experience/22-RESEARCH.md
@.planning/phases/22-recommendation-experience/22-PATTERNS.md
@.planning/phases/22-recommendation-experience/22-01-SUMMARY.md

<interfaces>
From src/lib/recommendations/types.ts (after Plan 01 extension):
```typescript
export type GuidancePackageItem = {
  // extends PortfolioRecommendationItem with:
  advisorPriority: AdvisorPriority | null;
  hiddenFromClient: boolean;
  taskStatus: TaskStatus;
  validationStatus: ValidationStatus;
  requiresValidation: boolean;
  deferredReason: string | null;
  deferredRevisitDate: Date | null;
  deferredTriggerEvent: string | null;
  responsibleRoles: string[];
  assignees: unknown;
  timeHorizon: string | null;
  assessmentSources: string[];
  mergedEvidence: unknown[];
};

export type GuidancePackage = {
  clientId: string;
  clientName: string;
  items: GuidancePackageItem[];
  summary: GuidancePackageSummary;
};
```

From src/lib/recommendations/solution-lifecycle.ts (current):
```typescript
const ALLOWED_TRANSITIONS: Record<RecommendationStatus, RecommendationStatus[]> = {
  PENDING: ["REVIEWED", "DECLINED"],
  REVIEWED: ["ACCEPTED", "DECLINED"],
  ACCEPTED: ["COMPLETED"],
  DECLINED: [],
  COMPLETED: [],
};
```

From src/lib/asset-catalog/inheritance-engine.ts (from Plan 01):
```typescript
export function composeAsset<T>(layers: AssetLayer<T>, policies: FieldOverridePolicy[]): ComposedAsset<T>;
export function validateOverlayPayload(fields: string[], policies: FieldOverridePolicy[]): { allowed: string[]; rejected: string[] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend lifecycle state machine with new transitions and DEFERRED/INCLUDED handling</name>
  <files>
    src/lib/recommendations/solution-lifecycle.ts
    src/lib/recommendations/solution-lifecycle.test.ts
  </files>
  <read_first>
    src/lib/recommendations/solution-lifecycle.ts (entire file -- current state machine, transition function, milestone hydration)
    src/lib/recommendations/compose-solution.test.ts (test pattern for factory functions)
    .planning/phases/22-recommendation-experience/22-RESEARCH.md (Pattern 2: Lifecycle State Machine Extension)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Extend ALLOWED_TRANSITIONS to include all new states while maintaining backward compatibility:
    - PENDING: ["REVIEWED", "DECLINED", "GENERATED"] -- backward compat + migration path
    - GENERATED: ["REVIEWED", "DEFERRED"]
    - REVIEWED: ["ACCEPTED", "DECLINED", "INCLUDED", "DEFERRED"] -- backward compat (ACCEPTED) + new (INCLUDED)
    - ACCEPTED: ["COMPLETED"] -- keep existing
    - INCLUDED: ["IN_PROGRESS", "DEFERRED"]
    - IN_PROGRESS: ["COMPLETED"]
    - DEFERRED: ["REVIEWED"] -- can be re-reviewed later (D-08)
    - DECLINED: [] -- terminal
    - COMPLETED: [] -- terminal

    Add new SOLUTION_ACTIONS constants: STATUS_GENERATED, STATUS_INCLUDED, STATUS_DEFERRED, STATUS_IN_PROGRESS. Add them to STATUS_ACTION_MAP.

    Extend transitionRecommendationStatus to handle new states:
    - On INCLUDED: compose solution and persist sourceLayerSummary (same as current ACCEPTED behavior), hydrate milestones
    - On DEFERRED: store deferredReason (required), deferredRevisitDate (optional), deferredTriggerEvent (optional) from input. Set deferredAt timestamp.
    - On IN_PROGRESS: set startedAt timestamp
    - On GENERATED: no special handling (initial state from engine)

    Add deferredAt DateTime? column handling in the switch statement. The input type for transitionRecommendationStatus needs extension: add optional deferredRevisitDate (Date), deferredTriggerEvent (string) fields.

    Extend or create solution-lifecycle.test.ts with unit tests:
    - Test GENERATED->REVIEWED transition succeeds
    - Test REVIEWED->INCLUDED transition succeeds
    - Test INCLUDED->IN_PROGRESS transition succeeds
    - Test REVIEWED->DEFERRED transition succeeds with reason
    - Test DEFERRED->REVIEWED transition succeeds (re-review)
    - Test GENERATED->COMPLETED transition fails (InvalidTransitionError)
    - Test backward compat: REVIEWED->ACCEPTED still works
    Note: These tests will need to mock prisma.$transaction. Use vi.mock for the prisma import. Follow the factory pattern from compose-solution.test.ts.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx vitest run src/lib/recommendations/solution-lifecycle.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ALLOWED_TRANSITIONS includes entries for GENERATED, INCLUDED, DEFERRED, IN_PROGRESS
    - SOLUTION_ACTIONS includes STATUS_GENERATED, STATUS_INCLUDED, STATUS_DEFERRED, STATUS_IN_PROGRESS
    - transitionRecommendationStatus handles INCLUDED same as ACCEPTED (compose + milestones)
    - transitionRecommendationStatus stores deferredReason, deferredRevisitDate, deferredTriggerEvent on DEFERRED transition
    - solution-lifecycle.test.ts has at least 6 test cases covering new transitions
    - All tests pass
  </acceptance_criteria>
  <done>State machine supports the full implementation-focused lifecycle with backward compatibility for existing PENDING/ACCEPTED flows.</done>
</task>

<task type="auto">
  <name>Task 2: Build per-client guidance package aggregation with cross-assessment deduplication</name>
  <files>
    src/lib/recommendations/guidance-package.ts
    src/lib/recommendations/guidance-package.test.ts
    src/lib/recommendations/compose-solution.ts
  </files>
  <read_first>
    src/lib/recommendations/solution-queries.ts (batch overlay fetching pattern -- getComposedSolutionsForAssessment)
    src/lib/recommendations/queries.ts (portfolio aggregation pattern -- getPortfolioRecommendations)
    src/lib/recommendations/compose-solution.ts (composeSolution function signature and behavior)
    src/lib/recommendations/types.ts (GuidancePackageItem, GuidancePackage, GuidancePackageSummary types)
    .planning/phases/22-recommendation-experience/22-RESEARCH.md (Pattern 1: Per-Client Guidance Package, Dedup Algorithm Sketch, Pitfall 1: N+1 Queries, Pitfall 3: Evidence Merging)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (guidance-package.ts analog section)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-01, D-02, D-04, D-23)
  </read_first>
  <action>
    Create src/lib/recommendations/guidance-package.ts with "server-only" import:

    Export getGuidancePackageForClient(clientId: string, advisorProfileId: string): Promise<GuidancePackage>
    Implementation flow:
    1. Query all completed assessments for clientId: prisma.assessment.findMany where userId=clientId, status="COMPLETED"
    2. Query all AssessmentRecommendation rows across those assessments with include serviceRecommendation, milestones. Order by urgencyScore desc.
    3. Deduplicate by serviceRecommendationId across assessments: group by serviceRecommendationId, keep the instance with the highest urgencyScore as primary, merge triggerReason arrays from all instances into mergedEvidence, collect all assessmentIds into assessmentSources.
    4. Resolve advisor context once: find ClientAdvisorAssignment for clientId where status="ACTIVE", get advisorProfile with enterprise context.
    5. Batch-fetch overlays: collect all serviceRecommendationIds, use Promise.all to fetch EnterpriseSolutionCustomization and AdvisorSolutionCustomization with { in: serviceIds } pattern (follow solution-queries.ts N+1 avoidance pattern).
    6. Compose each deduplicated recommendation using composeSolution with overlays from Maps.
    7. Build GuidancePackageItem for each: map composed solution fields + recommendation lifecycle fields (advisorPriority, taskStatus, validationStatus, hiddenFromClient, deferred fields, responsibleRoles, assignees, timeHorizon, assessmentSources, mergedEvidence).
    8. Compute GuidancePackageSummary: count totals by status.
    9. Resolve client display name using resolveClientDisplayName from "@/lib/signals/emit".

    Export deduplicateRecommendations as a separate pure function for testability:
    Input: AssessmentRecommendation[] (with serviceRecommendation included)
    Output: MergedRecommendation[] (with primaryId, serviceRecommendationId, urgencyScore, assessmentSources, mergedEvidence, status)

    Extend compose-solution.ts: add an optional overridePolicies parameter to composeSolution. When provided, use the generic composeAsset from the inheritance engine for the scalar override step. When not provided, use current last-writer-wins behavior (backward compatible). Import composeAsset from src/lib/asset-catalog/inheritance-engine.ts.

    Create guidance-package.test.ts with pure function tests:
    - Test deduplicateRecommendations: two recs with same serviceRecommendationId, different assessments -> single merged result with max urgency and unioned evidence
    - Test deduplicateRecommendations: three recs across two services -> two results
    - Test deduplicateRecommendations: single rec (no dedup needed) -> passes through unchanged
    - Test evidence merging: array and non-array triggerReason both handled
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx vitest run src/lib/recommendations/guidance-package.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/recommendations/guidance-package.ts exports getGuidancePackageForClient and deduplicateRecommendations
    - guidance-package.ts imports "server-only"
    - Deduplication groups by serviceRecommendationId, keeps highest urgencyScore, unions triggerReason arrays
    - Batch overlay fetch uses { in: serviceIds } pattern (no N+1)
    - compose-solution.ts has optional overridePolicies parameter with backward-compatible default
    - guidance-package.test.ts has at least 4 test cases for dedup logic
    - All tests pass (including existing compose-solution tests)
  </acceptance_criteria>
  <done>Per-client guidance package query aggregates across assessments with dedup. Composition engine optionally enforces override policies. Pure dedup function is tested independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisor -> guidance-package query | Advisor must only access clients assigned to them |
| cross-assessment data | Dedup must not leak data across different clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | guidance-package.ts | mitigate | getGuidancePackageForClient receives advisorProfileId; downstream plan actions validate advisor-client assignment before calling |
| T-22-04 | Tampering | compose-solution.ts | mitigate | Override policy enforcement rejects PROTECTED field mutations when overridePolicies provided |
</threat_model>

<verification>
- All lifecycle transition tests pass
- All guidance package dedup tests pass
- Existing compose-solution.test.ts continues to pass (backward compat)
- npm run test -- --run passes
</verification>

<success_criteria>
Lifecycle state machine supports the full D-07 flow. Per-client guidance package aggregation with cross-assessment dedup is functional and tested. Composition engine integrates with override policy enforcement.
</success_criteria>

<output>
Create `.planning/phases/22-recommendation-experience/22-02-SUMMARY.md` when done
</output>
