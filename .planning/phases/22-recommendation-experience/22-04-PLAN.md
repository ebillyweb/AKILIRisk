---
phase: 22-recommendation-experience
plan: "04"
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified:
  - src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx
  - src/components/guidance/GuidanceReviewPage.tsx
  - src/components/guidance/RecommendationCard.tsx
  - src/components/guidance/EvidenceAccordion.tsx
  - src/components/guidance/DeferDialog.tsx
  - src/components/guidance/BulkActionBar.tsx
  - src/components/guidance/GuidanceSummaryStrip.tsx
autonomous: false
requirements: [LIFECYCLE-01]

must_haves:
  truths:
    - "Advisor can view a holistic per-client guidance package across all assessments"
    - "Each recommendation shows insight, evidence, why it matters, layer attribution, and advisor controls"
    - "Advisor can include, defer, hide, adjust priority, and add notes to recommendations"
    - "Bulk actions allow selecting multiple recommendations and applying include or defer"
    - "Executive summary strip shows counts of included, deferred, completed items"
  artifacts:
    - path: "src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx"
      provides: "Advisor guidance review route"
    - path: "src/components/guidance/GuidanceReviewPage.tsx"
      provides: "Main guidance review layout with tabs and summary"
    - path: "src/components/guidance/RecommendationCard.tsx"
      provides: "Single recommendation card with all advisor controls"
    - path: "src/components/guidance/DeferDialog.tsx"
      provides: "Defer dialog with reason, revisit date, trigger event fields"
    - path: "src/components/guidance/BulkActionBar.tsx"
      provides: "Sticky bottom bulk action bar"
  key_links:
    - from: "src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx"
      to: "src/lib/recommendations/guidance-package.ts"
      via: "getGuidancePackageForClient call"
      pattern: "getGuidancePackageForClient"
    - from: "src/components/guidance/RecommendationCard.tsx"
      to: "src/lib/actions/guidance-actions.ts"
      via: "server action imports for Include/Defer/Hide/Priority"
      pattern: "includeInActionPlan|deferRecommendation|hideFromClient|adjustPriority"
---

<objective>
Build the advisor Guidance Review page (D-01, D-03, D-04, D-05, D-06, D-23) where advisors review and curate a holistic per-client guidance package. Install required shadcn components (accordion, sheet).

Purpose: This is the primary advisor workflow surface -- where advisors review AI-generated recommendations, accept or defer them, add notes, and curate the client's Strategic Action Plan. Without this, recommendations stay in "generated" status forever.

Output: Full advisor guidance review page with recommendation cards, evidence accordion, defer dialog, bulk actions, and summary strip.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recommendation-experience/22-CONTEXT.md
@.planning/phases/22-recommendation-experience/22-UI-SPEC.md
@.planning/phases/22-recommendation-experience/22-PATTERNS.md
@.planning/phases/22-recommendation-experience/22-02-SUMMARY.md
@.planning/phases/22-recommendation-experience/22-03-SUMMARY.md

<interfaces>
From src/lib/recommendations/guidance-package.ts (from Plan 02):
```typescript
export async function getGuidancePackageForClient(clientId: string, advisorProfileId: string): Promise<GuidancePackage>;
```

From src/lib/actions/guidance-actions.ts (from Plan 03):
```typescript
export async function includeInActionPlan(input: { recommendationIds: string[] }): Promise<ActionResult<{ results: { id: string; success: boolean; error?: string }[] }>>;
export async function deferRecommendation(input: DeferInput): Promise<ActionResult>;
export async function bulkDefer(input: BulkDeferInput): Promise<ActionResult>;
export async function hideFromClient(input: { recommendationId: string; hidden: boolean }): Promise<ActionResult>;
export async function adjustPriority(input: { recommendationId: string; priority: "HIGH" | "MEDIUM" | "LOW" }): Promise<ActionResult>;
export async function updateAdvisorNotes(input: { recommendationId: string; notes: string }): Promise<ActionResult>;
export async function updateValidationStatus(input: { recommendationId: string; status: "PENDING_REVIEW" | "VERIFIED" | "NEEDS_FOLLOWUP" }): Promise<ActionResult>;
export async function updateTimeHorizon(input: { recommendationId: string; timeHorizon: string }): Promise<ActionResult>;
export async function updateResponsibleRoles(input: { recommendationId: string; roles: string[] }): Promise<ActionResult>;
export async function updateAssignees(input: { recommendationId: string; assignees: { name: string; role?: string; org?: string }[] }): Promise<ActionResult>;
```

From src/lib/recommendations/types.ts:
```typescript
export type GuidancePackageItem = { /* all fields including lifecycle, deferred, roles, assignees */ };
export type GuidancePackage = { clientId, clientName, items, summary };
```

From src/components/advisor/AdvisorPageHeader.tsx (hero surface pattern):
```typescript
// Uses: icon + title + description layout in rounded-xl border card
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create route page with guidance review layout</name>
  <files>
    src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx
    src/components/guidance/GuidanceReviewPage.tsx
    src/components/guidance/GuidanceSummaryStrip.tsx
  </files>
  <read_first>
    src/app/(protected)/advisor/recommendations/page.tsx (exact analog -- hero surface pattern, server component with data loading, filter links, summary strip)
    src/components/recommendations/RecommendationsSummaryStrip.tsx (summary strip pattern to replicate)
    src/components/recommendations/RecommendationsPortfolio.tsx (card list layout pattern)
    .planning/phases/22-recommendation-experience/22-UI-SPEC.md (Advisor Guidance Review Page layout, Copywriting Contract for advisor section)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (advisor guidance route analog section)
  </read_first>
  <action>
    First, install shadcn accordion and sheet components:
    npx shadcn@latest add accordion sheet

    Create src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx:
    - Server component with params: { clientId: string }
    - Auth: requireAdvisorRole(), verify advisor has ClientAdvisorAssignment for clientId
    - Data: call getGuidancePackageForClient(clientId, advisorProfileId)
    - If no package or empty: render empty state per UI-SPEC copywriting: heading "No recommendations generated yet", body "Complete an assessment for this client to generate the guidance package. Start an assessment from the client's profile."
    - Render GuidanceReviewPage with the guidance package data
    - Hero surface follows the established pattern from advisor/recommendations/page.tsx:
      rounded-xl border card with icon (use Compass from lucide-react), page kicker "Client Guidance" (text-xs uppercase tracking-wide), page title = client name (text-2xl font-semibold font-display tracking-[-0.03em]), subtitle "Holistic guidance package synthesized across all completed assessments" (text-sm text-muted-foreground)

    Create src/components/guidance/GuidanceReviewPage.tsx as a client component ("use client"):
    - Accept guidance package data as prop
    - Render Tabs component with tabs: "Recommended Actions" (default), "Deferred", "Implementation Plan"
    - "Recommended Actions" tab: filter items where status is not DEFERRED and not hiddenFromClient, render as RecommendationCard list
    - "Deferred" tab: filter items where status is DEFERRED, section label "Future Considerations" per D-08
    - "Implementation Plan" tab: filter items where status is INCLUDED/IN_PROGRESS/COMPLETED, grouped by timeHorizon
    - Manage selected recommendation IDs state for bulk actions
    - Render BulkActionBar when selection count > 0

    Create src/components/guidance/GuidanceSummaryStrip.tsx:
    - Follow RecommendationsSummaryStrip.tsx pattern exactly
    - Display counts: Total, Included (with success badge color), Deferred (warning), Completed (success), In Progress (outline), Hidden (muted)
    - Render inside the hero surface card below the title
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/app/\(protected\)/advisor/clients/\[clientId\]/guidance/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/accordion.tsx exists (shadcn installed)
    - src/components/ui/sheet.tsx exists (shadcn installed)
    - Route page exists at src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx
    - Route page calls requireAdvisorRole() and verifies client assignment
    - Route page calls getGuidancePackageForClient
    - GuidanceReviewPage.tsx is a "use client" component with Tabs for "Recommended Actions", "Deferred", "Implementation Plan"
    - GuidanceSummaryStrip.tsx renders counts for included, deferred, completed, in progress, hidden items
    - Empty state uses exact copywriting from UI-SPEC
    - Hero surface uses font-display for page title, text-xs uppercase for kicker
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Advisor can navigate to /advisor/clients/[clientId]/guidance and see the guidance package with tabbed views and summary strip.</done>
</task>

<task type="auto">
  <name>Task 2: Build recommendation card, evidence accordion, defer dialog, and bulk action bar</name>
  <files>
    src/components/guidance/RecommendationCard.tsx
    src/components/guidance/EvidenceAccordion.tsx
    src/components/guidance/DeferDialog.tsx
    src/components/guidance/BulkActionBar.tsx
  </files>
  <read_first>
    src/components/assessment/FacilitatedRecommendations.tsx (card pattern -- recommendation display with badges, detail grid, advisor notes)
    src/components/ui/accordion.tsx (installed accordion component API)
    src/components/ui/dialog.tsx (existing dialog component API)
    .planning/phases/22-recommendation-experience/22-UI-SPEC.md (Interaction Contracts section -- all advisor controls, Defer Dialog copywriting, Hide from Client dialog, Bulk Actions, Priority badge colors, Lifecycle status badges)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-04, D-05, D-06, D-23)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (RecommendationCard.tsx and EvidenceAccordion.tsx analog sections)
  </read_first>
  <action>
    Create src/components/guidance/RecommendationCard.tsx ("use client"):
    - Accept GuidancePackageItem as prop, plus onSelect/selected for bulk checkbox
    - Top-right: Checkbox for bulk selection
    - Card header: Recommendation name (text-xl font-semibold), category (text-xs uppercase), priority badge per UI-SPEC (High: bg-destructive/10 text-destructive, Medium: bg-trust-accent/15 text-foreground, Low: outline variant)
    - Layer attribution row: Badge for each source layer present in the item's composed data. Platform: bg-muted text-muted-foreground. Enterprise: bg-secondary text-secondary-foreground. Advisor: bg-brand/15 text-brand-foreground. Each badge has aria-label="Source: [Layer]".
    - Status badge per UI-SPEC lifecycle mapping (Generated=secondary, Reviewed=outline, Included=success "In Action Plan", InProgress=warning "In Progress", Completed=success, Deferred=warning)
    - Body: description text (text-sm), then EvidenceAccordion showing trigger reasons
    - Advisor controls row per UI-SPEC Interaction Contracts:
      * Button variant="default" "Include in Action Plan" -- calls includeInActionPlan, optimistic UI badge transition. No confirmation dialog.
      * Button variant="outline" "Defer" -- opens DeferDialog
      * DropdownMenu trigger (ellipsis or "More"): "Mark Already Addressed" (immediate, transitions to COMPLETED), "Hide from Client" (opens confirmation Dialog per UI-SPEC: title "Hide from client?", destructive confirm "Yes, hide from client", cancel "Keep visible"), "Adjust Priority" submenu with High/Medium/Low
    - Inline Textarea for advisor notes: expands on click, auto-saves on blur via updateAdvisorNotes
    - All controls use useTransition for pending states
    - Announce status changes via aria-live="polite" on the status badge region

    Create src/components/guidance/EvidenceAccordion.tsx ("use client"):
    - Accept mergedEvidence array and assessmentSources
    - Render shadcn Accordion (collapsible, single item)
    - Trigger label: "Evidence from {N} assessment(s)" -- text-sm
    - Content: list of trigger reasons formatted with formatTriggerSummary from "@/lib/recommendations/format-trigger"
    - Each evidence item shows which assessment it came from (if multiple sources)

    Create src/components/guidance/DeferDialog.tsx ("use client"):
    - Accept recommendationId, onDefer callback, open/onOpenChange state
    - Use shadcn Dialog
    - Content per UI-SPEC Defer Dialog copywriting:
      Title "Defer this recommendation"
      Reason: Select from predefined reasons ["Not a current priority", "Requires prerequisite action", "Budget constraints", "Timing not right", "Needs more information", "Other"]
      Revisit date: Input type="date" with label "Revisit date (optional)"
      Trigger event: Input text with label "Trigger event (optional)", placeholder "e.g. After business valuation is complete"
      Notes: Textarea with label "Additional notes (optional)"
    - Confirm button "Confirm Defer", cancel "Back to recommendation"
    - On confirm: call deferRecommendation server action, close dialog

    Create src/components/guidance/BulkActionBar.tsx ("use client"):
    - Accept selectedIds array, onClearSelection, clientId
    - Fixed position at bottom of page (sticky bottom-0)
    - Show "{N} selected" count
    - "Include All" Button variant="default" -- calls includeInActionPlan with all selected IDs
    - "Defer All" Button variant="outline" -- opens single DeferDialog, applies same reason to all via bulkDefer
    - "Clear" Button variant="ghost" -- calls onClearSelection
    - Use useTransition for pending states on bulk actions
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/components/guidance/RecommendationCard.tsx src/components/guidance/EvidenceAccordion.tsx src/components/guidance/DeferDialog.tsx src/components/guidance/BulkActionBar.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - RecommendationCard.tsx renders name, category, priority badge, layer attribution badges, status badge, description, evidence accordion, and advisor controls row
    - RecommendationCard.tsx has "Include in Action Plan" Button, "Defer" Button, DropdownMenu with "Mark Already Addressed", "Hide from Client", "Adjust Priority"
    - "Hide from Client" shows confirmation Dialog with destructive variant confirm button
    - Layer attribution badges have aria-label="Source: Platform/Enterprise/Advisor"
    - Status badge region has aria-live="polite"
    - EvidenceAccordion.tsx uses shadcn Accordion with collapsible trigger
    - DeferDialog.tsx has reason Select, revisit date Input, trigger event Input, notes Textarea per UI-SPEC copywriting
    - BulkActionBar.tsx is fixed/sticky at bottom, shows count, has Include All and Defer All buttons
    - All components are "use client" components
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>All advisor guidance review interactive components are built with full control set, evidence display, defer workflow, and bulk actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Advisor Guidance Review page at /advisor/clients/[clientId]/guidance with:
    - Per-client holistic guidance package display
    - Recommendation cards with Include/Defer/Hide/Priority controls
    - Evidence accordion showing assessment trigger reasons
    - Defer dialog with reason/date/trigger fields
    - Bulk action bar for multi-select operations
    - Summary strip with status counts
    - Tab navigation (Recommended Actions / Deferred / Implementation Plan)
  </what-built>
  <how-to-verify>
    1. Sign in as advisor@test.com / Testpass1
    2. Navigate to a client with completed assessment (client@test.com)
    3. Visit /advisor/clients/[clientId]/guidance
    4. Verify: hero surface shows client name, kicker "Client Guidance", summary strip with counts
    5. Verify: recommendation cards show insight name, category, priority badge, layer attribution, status badge
    6. Click "Include in Action Plan" on a recommendation -- verify badge changes to "In Action Plan"
    7. Click "Defer" on another recommendation -- verify dialog opens with reason select, date, trigger fields
    8. Fill defer form and confirm -- verify recommendation moves to Deferred tab
    9. Select 2+ checkboxes -- verify bulk action bar appears at bottom
    10. Click the ellipsis/More menu -- verify "Mark Already Addressed", "Hide from Client", "Adjust Priority" options
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser -> server actions | All advisor controls submit via server actions with auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Spoofing | guidance route page.tsx | mitigate | requireAdvisorRole() + ClientAdvisorAssignment check before rendering |
| T-22-11 | Tampering | RecommendationCard.tsx | mitigate | All mutations go through server actions with ownership validation; no direct DB access from client components |
</threat_model>

<verification>
- TypeScript compilation passes for all components
- Route page loads without error for an advisor with assigned clients
- Checkpoint: human verification of the full advisor workflow
</verification>

<success_criteria>
Advisor can navigate to the per-client guidance review page, see recommendations with full context (insight, evidence, layer attribution), and use all curation controls (include, defer, hide, prioritize, notes, bulk actions).
</success_criteria>

<output>
Create `.planning/phases/22-recommendation-experience/22-04-SUMMARY.md` when done
</output>
