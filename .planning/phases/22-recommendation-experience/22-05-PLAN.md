---
phase: 22-recommendation-experience
plan: "05"
type: execute
wave: 3
depends_on: ["22-03"]
files_modified:
  - src/app/(protected)/advisor/enterprise/guidance/page.tsx
  - src/components/enterprise/GuidanceCustomization.tsx
  - src/components/enterprise/OverlayFieldEditor.tsx
  - src/components/enterprise/ComposedPreviewSheet.tsx
autonomous: false
requirements: [LIFECYCLE-01]

must_haves:
  truths:
    - "Enterprise admin can browse the platform recommendation catalog and create overlays"
    - "Platform definition is displayed as read-only alongside the editable enterprise overlay"
    - "Override policy prevents editing protected fields (insight, evidence, why it matters)"
    - "Preview composed result shows all three layers clearly attributed"
    - "Enterprise overlay saves custom guidance, vendors, compliance, playbooks, costs, timelines"
  artifacts:
    - path: "src/app/(protected)/advisor/enterprise/guidance/page.tsx"
      provides: "Enterprise guidance customization route"
    - path: "src/components/enterprise/GuidanceCustomization.tsx"
      provides: "Two-column overlay editor layout"
    - path: "src/components/enterprise/OverlayFieldEditor.tsx"
      provides: "Per-field overlay editor with policy badges"
    - path: "src/components/enterprise/ComposedPreviewSheet.tsx"
      provides: "Full composed preview in Sheet component"
  key_links:
    - from: "src/app/(protected)/advisor/enterprise/guidance/page.tsx"
      to: "src/lib/actions/enterprise-solution-actions.ts"
      via: "getEnterpriseOverlays and upsertEnterpriseOverlay"
      pattern: "getEnterpriseOverlays|upsertEnterpriseOverlay"
    - from: "src/components/enterprise/OverlayFieldEditor.tsx"
      to: "src/lib/recommendations/override-policy.ts"
      via: "field policy tier display"
      pattern: "RECOMMENDATION_FIELD_POLICIES"
---

<objective>
Build the enterprise guidance customization page (D-13, D-14, D-15) where enterprise admins browse the platform catalog and create overlays with firm-specific guidance, vendors, compliance requirements, and playbooks.

Purpose: Enterprise admins layer firm-specific guidance onto platform recommendations without copying or altering the underlying platform content. This is the inheritance visualization model -- platform definition alongside enterprise overlay.

Output: Enterprise guidance customization page with two-column layout, per-field overlay editor, and composed preview sheet.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recommendation-experience/22-CONTEXT.md
@.planning/phases/22-recommendation-experience/22-UI-SPEC.md
@.planning/phases/22-recommendation-experience/22-PATTERNS.md
@.planning/phases/22-recommendation-experience/22-03-SUMMARY.md

<interfaces>
From src/lib/actions/enterprise-solution-actions.ts (from Plan 03):
```typescript
export async function upsertEnterpriseOverlay(input: EnterpriseOverlayInput): Promise<ActionResult>;
export async function getEnterpriseOverlays(filters?: { serviceRecommendationId?: string }): Promise<ActionResult<EnterpriseOverlayData[]>>;
```

From src/lib/recommendations/override-policy.ts (from Plan 01):
```typescript
export const RECOMMENDATION_FIELD_POLICIES: Record<string, OverrideTier>;
export function getRecommendationPolicies(): FieldOverridePolicy[];
```

From src/lib/enterprise/team-access.ts:
```typescript
export async function requireEnterpriseTeamManager(userId: string): Promise<{ enterpriseId: string; enterpriseName: string; ... }>;
```

From src/lib/recommendations/compose-solution.ts:
```typescript
export function composeSolution(input: ComposeInput): ComposedSolution;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enterprise guidance route and two-column overlay editor</name>
  <files>
    src/app/(protected)/advisor/enterprise/guidance/page.tsx
    src/components/enterprise/GuidanceCustomization.tsx
    src/components/enterprise/OverlayFieldEditor.tsx
  </files>
  <read_first>
    src/app/(protected)/advisor/enterprise/recommendations/page.tsx (enterprise route pattern -- requireAdvisorRole, requireEnterpriseTeamManager, data loading, card grid layout)
    src/components/product-tour/ConfigurationPageHeader.tsx (header pattern used in enterprise pages)
    .planning/phases/22-recommendation-experience/22-UI-SPEC.md (Enterprise Guidance Customization Page layout, Interaction Contracts for enterprise overlay, Copywriting Contract for enterprise section)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-13, D-14, D-15)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (enterprise guidance route analog, OverlayFieldEditor no-analog section)
  </read_first>
  <action>
    Create src/app/(protected)/advisor/enterprise/guidance/page.tsx:
    - Server component with requireAdvisorRole() then requireEnterpriseTeamManager(userId)
    - Load all ServiceRecommendation entries from prisma with categories for list view
    - Load existing enterprise overlays via getEnterpriseOverlays
    - Render a list of platform recommendations as clickable cards (left nav or card grid per D-13 "browse catalog")
    - Hero surface: kicker "Enterprise Guidance", title "Guidance Customization", subtitle "Layer firm-specific guidance, preferred vendors, and compliance requirements onto platform recommendations" (from UI-SPEC copywriting)
    - Pass data to GuidanceCustomization component

    Create src/components/enterprise/GuidanceCustomization.tsx ("use client"):
    - Accept list of ServiceRecommendation + existing overlays
    - Left panel (40%): list of platform recommendations as selectable cards. Show name, category, description. Clicking one opens the detail editor on the right. Use bg-muted/40 for the left column background per UI-SPEC.
    - Right panel (60%): when a recommendation is selected, show two-column split:
      * Platform Definition column (read-only): Display immutable fields (name, description, expectedOutcome) with Badge variant="outline" reading "Platform -- Protected" per UI-SPEC. Show existing playbook steps, tags, prerequisites.
      * Enterprise Overlay column (editable): Form fields for D-14 configurable and addition fields.
    - Vertical Separator between columns per UI-SPEC
    - Form state managed with useState. Track dirty state for unsaved changes indicator.
    - Single "Save Overlay" Button at bottom of right column (primary accent per UI-SPEC)
    - On save: call upsertEnterpriseOverlay server action
    - "Preview Composed Result" Button variant="outline" -- opens ComposedPreviewSheet
    - Empty overlay state when no overlay exists: "No overlay configured. Add firm guidance, preferred vendors, or compliance requirements using the fields on the right." (from UI-SPEC)

    Create src/components/enterprise/OverlayFieldEditor.tsx ("use client"):
    - Accept field name, current platform value (read-only), current overlay value (editable), override tier from RECOMMENDATION_FIELD_POLICIES
    - Render the appropriate input control based on field type:
      * String fields (customGuidance, complianceDisclosures, notes): Textarea
      * Cost/timeframe/provider: Input
      * isRequired: Checkbox
      * priorityAdjustment: number Input (-10 to +10)
      * internalLinks: dynamic array of {url, label} inputs with add/remove
      * additionalPlaybook: dynamic array of step inputs {title, description, duration, sortOrder} with add/remove
    - Display field tier badge: "Protected" (muted, disabled), "Configurable" (outline), "Enterprise Addition" (secondary)
    - PROTECTED fields show platform value with locked icon and "Platform -- Protected" badge, no edit controls
    - CONFIGURABLE fields show platform value as placeholder/reference with editable overlay field
    - ADDITION fields show only the editable field (no platform reference needed)
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/app/\(protected\)/advisor/enterprise/guidance/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Enterprise guidance route exists at src/app/(protected)/advisor/enterprise/guidance/page.tsx
    - Route uses requireAdvisorRole + requireEnterpriseTeamManager auth guard
    - GuidanceCustomization.tsx renders left panel (catalog browse, bg-muted/40) and right panel (overlay editor)
    - Platform Definition displays immutable fields with "Platform -- Protected" Badge
    - Enterprise Overlay displays editable form fields for all D-14 items
    - OverlayFieldEditor.tsx renders PROTECTED fields as read-only with lock icon
    - OverlayFieldEditor.tsx renders CONFIGURABLE fields with editable overlay alongside platform reference
    - "Save Overlay" button calls upsertEnterpriseOverlay
    - Hero surface uses exact UI-SPEC copywriting
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Enterprise guidance customization page renders two-column layout with read-only platform definition and editable enterprise overlay with field-level policy enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Build composed preview sheet</name>
  <files>
    src/components/enterprise/ComposedPreviewSheet.tsx
  </files>
  <read_first>
    src/components/ui/sheet.tsx (Sheet component API -- installed in Plan 04 Task 1)
    src/lib/recommendations/compose-solution.ts (composeSolution function and ComposedSolution type)
    .planning/phases/22-recommendation-experience/22-UI-SPEC.md (Enterprise Overlay interaction contracts -- "Preview Composed Result" section)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-15, D-23)
  </read_first>
  <action>
    Create src/components/enterprise/ComposedPreviewSheet.tsx ("use client"):
    - Accept open/onOpenChange state, ServiceRecommendation data, enterprise overlay form values (current unsaved state from the form), and optionally advisor customization for full 3-layer preview
    - Use shadcn Sheet component (side="right", wide enough for content -- className="sm:max-w-xl")
    - Compose the preview client-side by applying the overlay values on top of platform values. For preview purposes, do the composition locally (no server round-trip needed -- just merge the form state with the platform data).
    - Display the composed result as read-only cards:
      * Name and description (platform -- always)
      * Estimated Cost, Timeframe, Provider -- show composed value with source attribution label
      * Playbook: combined ordered list with source badges (Platform/Enterprise/Advisor) per step
      * Notes: collected from all layers
      * Compliance disclosures (if enterprise provided)
      * Internal links (if enterprise provided)
    - Each field shows a small source attribution tag: "Platform", "Enterprise", or "Advisor" using the same badge colors as the guidance review page (bg-muted for platform, bg-secondary for enterprise, bg-brand/15 for advisor)
    - Sheet is read-only -- no edit controls inside the preview
    - Header: "Composed Preview" with Sheet close button
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/components/enterprise/ComposedPreviewSheet.tsx 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ComposedPreviewSheet.tsx uses shadcn Sheet component
    - Preview shows composed result from all layers with source attribution badges
    - Each field has source attribution tag (Platform/Enterprise/Advisor)
    - Playbook shows combined steps from all layers with source badges
    - Sheet is read-only (no edit controls)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Enterprise admin can preview the fully composed recommendation as seen by advisors and clients, with clear source attribution per field.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Enterprise Guidance Customization page at /advisor/enterprise/guidance with:
    - Catalog browse (list of platform recommendations)
    - Two-column layout: read-only Platform Definition | editable Enterprise Overlay
    - Per-field policy enforcement (Protected fields locked, Configurable editable, Additions appendable)
    - Save overlay functionality
    - Composed preview sheet showing all three layers with attribution
  </what-built>
  <how-to-verify>
    1. Sign in as advisor@test.com / Testpass1 (must have enterprise team manager role)
    2. Navigate to /advisor/enterprise/guidance
    3. Verify: hero surface shows "Enterprise Guidance" kicker, "Guidance Customization" title
    4. Click on a platform recommendation in the catalog list
    5. Verify: left column shows Platform Definition with "Platform -- Protected" badges on immutable fields
    6. Verify: right column shows editable overlay fields (custom guidance, vendors, compliance, cost, timeframe)
    7. Enter values in the overlay fields and click "Save Overlay"
    8. Click "Preview Composed Result" -- verify Sheet opens showing composed view with source attribution
    9. Verify: PROTECTED fields (name, description) cannot be edited in the overlay column
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enterprise admin -> overlay mutations | Only enterprise team managers can modify overlays |
| overlay fields -> override policy | PROTECTED fields must not be writable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-12 | Tampering | OverlayFieldEditor.tsx | mitigate | UI disables PROTECTED fields; server-side validateOverlayFields rejects them even if client-side bypass attempted |
| T-22-13 | Information Disclosure | enterprise guidance route | mitigate | requireEnterpriseTeamManager scopes all data to the authenticated enterprise |
</threat_model>

<verification>
- TypeScript compilation passes for all enterprise components
- Checkpoint: human verification of enterprise overlay workflow
</verification>

<success_criteria>
Enterprise admin can browse the platform catalog, create overlays with firm-specific guidance, see clear distinction between platform (protected) and enterprise (editable) fields, and preview the composed result with full source attribution.
</success_criteria>

<output>
Create `.planning/phases/22-recommendation-experience/22-05-SUMMARY.md` when done
</output>
