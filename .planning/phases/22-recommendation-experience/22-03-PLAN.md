---
phase: 22-recommendation-experience
plan: "03"
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/lib/actions/guidance-schemas.ts
  - src/lib/actions/guidance-actions.ts
  - src/lib/actions/enterprise-solution-actions.ts
  - src/lib/actions/client-action-plan-actions.ts
  - src/lib/actions/guidance-actions.test.ts
  - src/lib/actions/enterprise-solution-actions.test.ts
  - src/lib/actions/client-action-plan-actions.test.ts
autonomous: true
requirements: [LIFECYCLE-01]

must_haves:
  truths:
    - "Advisor can include, defer, hide, adjust priority, and add notes to recommendations via server actions"
    - "Enterprise admin can create and update solution overlays with override policy enforcement"
    - "Client can update task status on included recommendations"
    - "Advisor can update validation status on recommendations"
    - "All mutations are ownership-validated and audit-logged"
    - "Ownership validation, override policy enforcement, and bulk transaction logic are tested"
  artifacts:
    - path: "src/lib/actions/guidance-actions.ts"
      provides: "Advisor guidance review server actions"
      exports: ["includeInActionPlan", "deferRecommendation", "hideFromClient", "adjustPriority", "updateAdvisorNotes", "updateValidationStatus", "bulkInclude", "bulkDefer", "updateTimeHorizon", "updateResponsibleRoles", "updateAssignees"]
    - path: "src/lib/actions/enterprise-solution-actions.ts"
      provides: "Enterprise overlay CRUD server actions"
      exports: ["upsertEnterpriseOverlay", "getEnterpriseOverlays"]
    - path: "src/lib/actions/client-action-plan-actions.ts"
      provides: "Client task status update server action"
      exports: ["updateTaskStatus", "getClientActionPlan"]
    - path: "src/lib/actions/guidance-schemas.ts"
      provides: "Zod validation schemas for all guidance actions"
      exports: ["includeSchema", "deferSchema", "bulkActionSchema", "enterpriseOverlaySchema", "taskStatusSchema"]
    - path: "src/lib/actions/guidance-actions.test.ts"
      provides: "Tests for advisor guidance actions ownership validation and bulk transactions"
    - path: "src/lib/actions/enterprise-solution-actions.test.ts"
      provides: "Tests for enterprise overlay policy enforcement"
    - path: "src/lib/actions/client-action-plan-actions.test.ts"
      provides: "Tests for client task status ownership checks"
  key_links:
    - from: "src/lib/actions/guidance-actions.ts"
      to: "src/lib/recommendations/solution-lifecycle.ts"
      via: "transitionRecommendationStatus for status changes"
      pattern: "transitionRecommendationStatus"
    - from: "src/lib/actions/enterprise-solution-actions.ts"
      to: "src/lib/recommendations/override-policy.ts"
      via: "validateOverlayFields before saving"
      pattern: "validateOverlayFields"
---

<objective>
Build all server actions for the three-role recommendation experience: advisor guidance review actions (D-05, D-06), enterprise overlay CRUD (D-14), and client task status updates (D-21). Includes tests for ownership validation, override policy enforcement, and bulk transaction atomicity.

Purpose: Server actions are the mutation layer consumed by all three UI surfaces. They enforce ownership, validate input with Zod, use the lifecycle state machine for transitions, and respect override policies for enterprise overlays.

Output: Three server action files with full CRUD coverage, one shared Zod schema file, three test files covering security-critical paths, all ownership-validated and audit-logged.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recommendation-experience/22-CONTEXT.md
@.planning/phases/22-recommendation-experience/22-RESEARCH.md
@.planning/phases/22-recommendation-experience/22-PATTERNS.md
@.planning/phases/22-recommendation-experience/22-01-SUMMARY.md

<interfaces>
From src/lib/actions/admin-recommendation-actions.ts (action pattern):
```typescript
"use server";
export type ActionResult<T = void> =
  | { success: true; data: T }
  | { success: false; error: string; code?: string };
```

From src/lib/advisor/auth.ts:
```typescript
export async function requireAdvisorRole(): Promise<{ userId: string; role: string; email: string | null }>;
```

From src/lib/enterprise/team-access.ts:
```typescript
export async function requireEnterpriseTeamManager(userId: string): Promise<{ enterpriseId: string; enterpriseName: string; ... }>;
```

From src/lib/recommendations/solution-lifecycle.ts (after Plan 02):
```typescript
export async function transitionRecommendationStatus(input: {
  recommendationId: string;
  newStatus: RecommendationStatus;
  actorId: string;
  reason?: string;
  notes?: string;
  deferredRevisitDate?: Date;
  deferredTriggerEvent?: string;
}): Promise<void>;
```

From src/lib/recommendations/override-policy.ts (from Plan 01):
```typescript
export function validateOverlayFields(fields: string[]): { allowed: string[]; rejected: string[] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zod validation schemas and ownership helpers</name>
  <files>
    src/lib/actions/guidance-schemas.ts
  </files>
  <read_first>
    src/lib/actions/admin-recommendation-actions.ts (ActionResult pattern, Zod validation patterns)
    src/lib/admin/recommendation-rule-schemas.ts (Zod schema pattern reference)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-05, D-06, D-08, D-14, D-20, D-21)
  </read_first>
  <action>
    Create src/lib/actions/guidance-schemas.ts with all Zod schemas shared by the three action files:

    Advisor schemas:
    - includeSchema: z.object({ recommendationIds: z.array(z.string().cuid()).min(1).max(100) })
    - deferSchema: z.object({ recommendationId: z.string().cuid(), reason: z.string().min(1).max(500), revisitDate: z.string().datetime().optional(), triggerEvent: z.string().max(300).optional(), notes: z.string().max(2000).optional() })
    - bulkDeferSchema: z.object({ recommendationIds: z.array(z.string().cuid()).min(1).max(100), reason: z.string().min(1).max(500), revisitDate: z.string().datetime().optional() })
    - hideFromClientSchema: z.object({ recommendationId: z.string().cuid(), hidden: z.boolean() })
    - adjustPrioritySchema: z.object({ recommendationId: z.string().cuid(), priority: z.enum(["HIGH", "MEDIUM", "LOW"]) })
    - updateNotesSchema: z.object({ recommendationId: z.string().cuid(), notes: z.string().max(5000) })
    - validationStatusSchema: z.object({ recommendationId: z.string().cuid(), status: z.enum(["PENDING_REVIEW", "VERIFIED", "NEEDS_FOLLOWUP"]) })
    - updateTimeHorizonSchema: z.object({ recommendationId: z.string().cuid(), timeHorizon: z.enum(["immediate", "strategic", "ongoing"]) })
    - updateRolesSchema: z.object({ recommendationId: z.string().cuid(), roles: z.array(z.string().max(100)).max(10) })
    - updateAssigneesSchema: z.object({ recommendationId: z.string().cuid(), assignees: z.array(z.object({ name: z.string(), role: z.string().optional(), org: z.string().optional() })).max(20) })

    Enterprise schema:
    - enterpriseOverlaySchema: z.object({ serviceRecommendationId: z.string().cuid(), isRequired: z.boolean().optional(), priorityAdjustment: z.number().int().min(-10).max(10).optional(), complianceDisclosures: z.string().max(5000).optional(), customGuidance: z.string().max(5000).optional(), internalLinks: z.array(z.object({ url: z.string().url(), label: z.string() })).max(20).optional(), costOverride: z.string().max(200).optional(), timeframeOverride: z.string().max(200).optional(), providerOverride: z.string().max(200).optional(), notes: z.string().max(5000).optional(), additionalPlaybook: z.object({ steps: z.array(z.object({ title: z.string(), description: z.string().optional(), estimatedDuration: z.string().optional(), sortOrder: z.number().int() })).max(20) }).optional(), isActive: z.boolean().optional() })

    Client schema:
    - taskStatusSchema: z.object({ recommendationId: z.string().cuid(), taskStatus: z.enum(["NOT_STARTED", "IN_PROGRESS", "WAITING", "READY_FOR_REVIEW", "COMPLETED"]) })

    Export all schemas as named exports.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/lib/actions/guidance-schemas.ts 2>&1 | head -10</automated>
  </verify>
  <done>All Zod validation schemas are defined and exported for consumption by the three action files.</done>
</task>

<task type="auto">
  <name>Task 2: Create advisor guidance review and enterprise/client server actions with tests</name>
  <files>
    src/lib/actions/guidance-actions.ts
    src/lib/actions/enterprise-solution-actions.ts
    src/lib/actions/client-action-plan-actions.ts
    src/lib/actions/guidance-actions.test.ts
    src/lib/actions/enterprise-solution-actions.test.ts
    src/lib/actions/client-action-plan-actions.test.ts
  </files>
  <read_first>
    src/lib/actions/admin-recommendation-actions.ts (ActionResult pattern, Zod validation, audit logging, revalidatePath)
    src/lib/actions/enterprise-recommendation-actions.ts (enterprise auth pattern with requireEnterpriseTeamManager, advisorHubActionErrorMessage)
    src/lib/advisor/auth.ts (requireAdvisorRole signature -- lines 193-219)
    src/lib/enterprise/team-access.ts (requireEnterpriseTeamManager signature)
    src/lib/recommendations/solution-lifecycle.ts (transitionRecommendationStatus signature)
    src/lib/recommendations/override-policy.ts (validateOverlayFields for policy enforcement)
    src/lib/auth.ts (auth() session pattern for client actions)
    src/app/(protected)/dashboard/page.tsx (client auth pattern -- lines 37-50)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (all three action file analogs, shared auth patterns)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-05, D-06, D-08, D-13, D-14, D-20, D-21, D-22)
    .planning/phases/22-recommendation-experience/22-RESEARCH.md (Pitfall 4: Race Condition on Bulk Actions, Pitfall 5: Override Policy Bypass)
  </read_first>
  <action>
    Create src/lib/actions/guidance-actions.ts with "use server" directive:
    - Use ActionResult<T> pattern from admin-recommendation-actions.ts (ok/fail helpers)
    - Import requireAdvisorRole from "@/lib/advisor/auth"
    - Import transitionRecommendationStatus from "@/lib/recommendations/solution-lifecycle"
    - Import schemas from "./guidance-schemas"

    Create a shared helper verifyAdvisorOwnsRecommendation(advisorUserId: string, recommendationId: string): Promise<boolean> that joins AssessmentRecommendation -> Assessment (userId) -> ClientAdvisorAssignment (clientId=userId, advisorId from AdvisorProfile where userId=advisorUserId, status="ACTIVE"). Export for reuse.

    Create verifyAdvisorOwnsRecommendations(advisorUserId: string, recommendationIds: string[]): Promise<{ valid: string[]; unauthorized: string[] }> for bulk operations.

    Implement advisor actions (each validates with Zod, checks advisor ownership, uses try/catch with logSafeError):

    includeInActionPlan(input): Accepts includeSchema. Verify each recommendationId's assessment belongs to an assigned client (single query joining AssessmentRecommendation->Assessment->ClientAdvisorAssignment). Process all in a single prisma.$transaction to avoid Pitfall 4. Call transitionRecommendationStatus for each with newStatus "INCLUDED". Return per-item results { id, success, error? }.

    deferRecommendation(input): Accepts deferSchema. Verify ownership. Call transitionRecommendationStatus with newStatus "DEFERRED", passing reason, deferredRevisitDate (parse from ISO string), deferredTriggerEvent.

    bulkDefer(input): Accepts bulkDeferSchema. Same ownership check. Single transaction, apply same reason to all.

    hideFromClient(input): Accepts hideFromClientSchema. Verify ownership. Direct Prisma update on AssessmentRecommendation setting hiddenFromClient. Log SolutionActivity with action "hide_from_client" or "unhide_from_client".

    adjustPriority(input): Accepts adjustPrioritySchema. Verify ownership. Direct Prisma update setting advisorPriority. Log activity.

    updateAdvisorNotes(input): Accepts updateNotesSchema. Verify ownership. Direct Prisma update setting advisorNotes. No activity log (notes are editorial, not lifecycle events).

    updateValidationStatus(input): Accepts validationStatusSchema. Verify ownership. Direct Prisma update setting validationStatus. Log activity with action "validation_status_change".

    updateTimeHorizon(input): Accepts updateTimeHorizonSchema. Verify ownership. Direct Prisma update.

    updateResponsibleRoles(input): Accepts updateRolesSchema. Verify ownership. Direct Prisma update setting responsibleRoles.

    updateAssignees(input): Accepts updateAssigneesSchema. Verify ownership. Direct Prisma update setting assignees Json.

    All actions call revalidatePath("/advisor/clients") after mutation.

    Create src/lib/actions/enterprise-solution-actions.ts with "use server" directive:
    - Import requireAdvisorRole and requireEnterpriseTeamManager
    - Import validateOverlayFields from "@/lib/recommendations/override-policy"
    - Import enterpriseOverlaySchema from "./guidance-schemas"
    - Use the same error handling pattern as enterprise-recommendation-actions.ts (advisorHubActionErrorMessage)

    upsertEnterpriseOverlay(input): Accepts enterpriseOverlaySchema. Auth via requireAdvisorRole then requireEnterpriseTeamManager(userId). Before writing, call validateOverlayFields on the input field names to prevent PROTECTED field writes per Pitfall 5. Use prisma.enterpriseSolutionCustomization.upsert with the unique constraint [enterpriseId, serviceRecommendationId]. Only write the new D-14 fields (isRequired, priorityAdjustment, complianceDisclosures, customGuidance, internalLinks) plus existing overlay fields (costOverride, timeframeOverride, providerOverride, additionalPlaybook, notes, isActive). Revalidate "/advisor/enterprise/guidance".

    getEnterpriseOverlays(filters?: { serviceRecommendationId?: string }): Auth via requireAdvisorRole then requireEnterpriseTeamManager. Query EnterpriseSolutionCustomization scoped to team.enterpriseId. Include serviceRecommendation for display context. Return ActionResult with overlay data.

    Create src/lib/actions/client-action-plan-actions.ts with "use server" directive:
    - Import auth from "@/lib/auth"
    - Import taskStatusSchema from "./guidance-schemas"
    - Use ActionResult pattern but with session-based auth (not requireAdvisorRole)

    updateTaskStatus(input): Accepts taskStatusSchema. Auth via auth() session. Verify the recommendation's assessment.userId matches session.user.id (critical for row-level isolation). Direct Prisma update setting taskStatus on AssessmentRecommendation. If taskStatus is "COMPLETED" and requiresValidation is true, create a SolutionActivity with action "validation_requested". Revalidate "/dashboard/action-plan".

    getClientActionPlan(): Auth via auth() session. Query all AssessmentRecommendation rows for this client across all completed assessments where status is INCLUDED, IN_PROGRESS, or COMPLETED, and hiddenFromClient is false. Group by timeHorizon (immediate/strategic/ongoing, with null defaulting to "strategic"). Include serviceRecommendation for display context. Return the grouped data matching the Strategic Action Plan structure (D-18).

    Both enterprise and client files MUST NOT expose validationStatus mutations -- that is advisor-only in guidance-actions.ts.

    Create src/lib/actions/guidance-actions.test.ts:
    - Mock prisma, requireAdvisorRole, transitionRecommendationStatus using vi.mock
    - Test: verifyAdvisorOwnsRecommendation returns true for valid assignment, false for unauthorized
    - Test: verifyAdvisorOwnsRecommendations returns correct valid/unauthorized split
    - Test: includeInActionPlan rejects when advisor does not own the recommendation (returns error ActionResult)
    - Test: includeInActionPlan uses prisma.$transaction for bulk atomicity
    - Test: deferRecommendation stores reason, revisitDate, triggerEvent via transitionRecommendationStatus
    - Test: hideFromClient creates SolutionActivity audit record

    Create src/lib/actions/enterprise-solution-actions.test.ts:
    - Mock prisma, requireAdvisorRole, requireEnterpriseTeamManager, validateOverlayFields using vi.mock
    - Test: upsertEnterpriseOverlay rejects when validateOverlayFields reports rejected fields (override policy enforcement)
    - Test: upsertEnterpriseOverlay scopes upsert to team.enterpriseId
    - Test: getEnterpriseOverlays scopes query to authenticated enterprise

    Create src/lib/actions/client-action-plan-actions.test.ts:
    - Mock prisma, auth using vi.mock
    - Test: updateTaskStatus rejects when assessment.userId does not match session.user.id
    - Test: updateTaskStatus creates validation_requested activity when COMPLETED + requiresValidation
    - Test: getClientActionPlan excludes hiddenFromClient=true items
    - Test: getClientActionPlan only returns INCLUDED/IN_PROGRESS/COMPLETED status recommendations
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx vitest run src/lib/actions/guidance-actions.test.ts src/lib/actions/enterprise-solution-actions.test.ts src/lib/actions/client-action-plan-actions.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actions/guidance-actions.ts has "use server" directive and exports all 11 advisor actions
    - Each action calls requireAdvisorRole() and verifies ownership before mutation
    - includeInActionPlan and bulkDefer use prisma.$transaction for atomicity
    - All actions return ActionResult<T>
    - enterprise-solution-actions.ts exports upsertEnterpriseOverlay and getEnterpriseOverlays
    - upsertEnterpriseOverlay calls validateOverlayFields before writing (policy enforcement)
    - upsertEnterpriseOverlay scopes all queries to team.enterpriseId
    - client-action-plan-actions.ts exports updateTaskStatus and getClientActionPlan
    - updateTaskStatus verifies assessment.userId === session.user.id before mutation
    - getClientActionPlan filters out hiddenFromClient=true and only returns INCLUDED/IN_PROGRESS/COMPLETED
    - Neither enterprise nor client file exposes validationStatus mutations
    - guidance-actions.test.ts has at least 6 test cases covering ownership and bulk
    - enterprise-solution-actions.test.ts has at least 3 test cases covering policy enforcement
    - client-action-plan-actions.test.ts has at least 4 test cases covering ownership and filtering
    - All tests pass
    - TypeScript compilation passes for all files
  </acceptance_criteria>
  <done>All three role-specific server action files are implemented with proper auth, ownership validation, Zod input validation, override policy enforcement, and audit logging. Tests cover ownership validation, override policy enforcement, and bulk transaction logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisor -> client data | Advisor must only access their assigned clients' recommendations |
| enterprise -> overlay data | Enterprise admin must only modify their own enterprise's overlays |
| client -> recommendation data | Client must only update their own assessment recommendations |
| enterprise -> override policy | Enterprise mutations must respect PROTECTED field restrictions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information Disclosure | guidance-actions.ts | mitigate | verifyAdvisorOwnsRecommendation checks ClientAdvisorAssignment before every data access |
| T-22-06 | Tampering | enterprise-solution-actions.ts | mitigate | requireEnterpriseTeamManager + enterpriseId scoping on all queries; validateOverlayFields rejects PROTECTED writes |
| T-22-07 | Elevation of Privilege | client-action-plan-actions.ts | mitigate | assessment.userId === session.user.id check before task status mutation |
| T-22-08 | Tampering | guidance-actions.ts bulk actions | mitigate | verifyAdvisorOwnsRecommendations validates every ID in bulk array; single transaction |
| T-22-09 | Elevation of Privilege | client-action-plan-actions.ts | mitigate | validationStatus mutations not exposed in client actions -- only in advisor guidance-actions.ts |
</threat_model>

<verification>
- TypeScript compilation passes for all 4 production files
- All 13+ test cases pass across 3 test files
- Each action file has proper auth guards
- npm run test -- --run passes (no regressions)
</verification>

<success_criteria>
All three role-specific server action files are implemented with proper auth, ownership validation, Zod input validation, override policy enforcement, and audit logging. Bulk operations use transactions. Tests verify ownership validation, policy enforcement, and bulk atomicity.
</success_criteria>

<output>
Create `.planning/phases/22-recommendation-experience/22-03-SUMMARY.md` when done
</output>
