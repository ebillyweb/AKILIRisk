---
phase: 22-recommendation-experience
plan: "06"
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified:
  - src/app/(protected)/dashboard/action-plan/page.tsx
  - src/components/action-plan/StrategicActionPlan.tsx
  - src/components/action-plan/ExecutiveSummary.tsx
  - src/components/action-plan/ActionCard.tsx
  - src/components/action-plan/TimeHorizonSection.tsx
  - src/components/action-plan/ProgressDashboard.tsx
autonomous: false
requirements: [LIFECYCLE-01]

must_haves:
  truths:
    - "Client sees a personalized Strategic Action Plan with accepted recommendations"
    - "Action plan is organized by time horizon: Immediate Priorities (0-90d), Strategic Initiatives (3-12mo), Ongoing Practices"
    - "Each action preserves the full chain of reasoning: why recommended, expected benefit, implementation guidance"
    - "Client can update task status on each action (Not Started, In Progress, Waiting, Ready for Review, Completed)"
    - "Progress dashboard shows completion percentage and per-recommendation progress bars"
    - "Hidden-from-client recommendations do not appear"
  artifacts:
    - path: "src/app/(protected)/dashboard/action-plan/page.tsx"
      provides: "Client Strategic Action Plan route"
    - path: "src/components/action-plan/StrategicActionPlan.tsx"
      provides: "Main action plan layout with sections"
    - path: "src/components/action-plan/ExecutiveSummary.tsx"
      provides: "Hero summary card with readiness score and top priorities"
    - path: "src/components/action-plan/ActionCard.tsx"
      provides: "Single action card with status selector and reasoning chain"
    - path: "src/components/action-plan/ProgressDashboard.tsx"
      provides: "Progress bars and completion percentages"
  key_links:
    - from: "src/app/(protected)/dashboard/action-plan/page.tsx"
      to: "src/lib/actions/client-action-plan-actions.ts"
      via: "getClientActionPlan call"
      pattern: "getClientActionPlan"
    - from: "src/components/action-plan/ActionCard.tsx"
      to: "src/lib/actions/client-action-plan-actions.ts"
      via: "updateTaskStatus server action"
      pattern: "updateTaskStatus"
---

<objective>
Build the client Strategic Action Plan page (D-16, D-17, D-18, D-19, D-21) where clients see their personalized roadmap of included recommendations organized by time horizon with full reasoning chains and task status tracking.

Purpose: This is the client-facing outcome of the entire recommendation experience. The client leaves the assessment knowing exactly what to do next, why it matters, and how to track progress. The name "Strategic Action Plan" (per D-16) positions the platform as an advisory tool, not a task manager.

Output: Client Strategic Action Plan page with executive summary, time-horizon sections, action cards with reasoning chains, task status selectors, and progress dashboard.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recommendation-experience/22-CONTEXT.md
@.planning/phases/22-recommendation-experience/22-UI-SPEC.md
@.planning/phases/22-recommendation-experience/22-PATTERNS.md
@.planning/phases/22-recommendation-experience/22-03-SUMMARY.md

<interfaces>
From src/lib/actions/client-action-plan-actions.ts (from Plan 03):
```typescript
export async function updateTaskStatus(input: { recommendationId: string; taskStatus: TaskStatus }): Promise<ActionResult>;
export async function getClientActionPlan(): Promise<ActionResult<ClientActionPlanData>>;
// ClientActionPlanData groups recommendations by timeHorizon (immediate/strategic/ongoing)
// Each item includes: name, description, expectedOutcome, estimatedCost, timeframe, provider,
// playbook steps, taskStatus, validationStatus, requiresValidation, responsibleRoles, assignees,
// mergedEvidence/triggerReason, sourceLayer (composed)
```

From src/lib/auth.ts:
```typescript
export async function auth(): Promise<Session | null>;
```

From src/lib/recommendations/types.ts:
```typescript
export type GuidancePackageItem = { /* all fields */ };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client Strategic Action Plan route and main layout components</name>
  <files>
    src/app/(protected)/dashboard/action-plan/page.tsx
    src/components/action-plan/StrategicActionPlan.tsx
    src/components/action-plan/ExecutiveSummary.tsx
    src/components/action-plan/TimeHorizonSection.tsx
  </files>
  <read_first>
    src/app/(protected)/dashboard/page.tsx (client dashboard auth pattern -- session check, role-based redirect, data loading)
    src/components/assessment/FacilitatedRecommendations.tsx (existing client-facing recommendation display pattern)
    .planning/phases/22-recommendation-experience/22-UI-SPEC.md (Client Strategic Action Plan Page layout, Copywriting Contract for client section, State Definitions for status badges)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-16, D-17, D-18, D-19)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (client action plan route analog, StrategicActionPlan.tsx analog)
  </read_first>
  <action>
    Create src/app/(protected)/dashboard/action-plan/page.tsx:
    - Server component with auth() session check. If no session, return null. If ADVISOR/ADMIN role, redirect to /advisor or /admin.
    - Call getClientActionPlan() server action to load the client's action plan data
    - If no data or empty: render empty state per UI-SPEC: heading "Your action plan is being prepared", body "Your advisor is reviewing your assessment results and will publish your Strategic Action Plan shortly."
    - If error: render Alert variant="destructive" with "Unable to load your action plan. Please try refreshing. If this continues, contact your advisor."
    - Page title: "Your Strategic Action Plan" per UI-SPEC (text-2xl font-semibold font-display tracking-[-0.03em])
    - Pass data to StrategicActionPlan component

    Create src/components/action-plan/StrategicActionPlan.tsx ("use client"):
    - Accept action plan data (grouped by timeHorizon)
    - Render vertical stack with 2xl (48px) spacing between major sections per UI-SPEC:
      1. ExecutiveSummary hero card
      2. TimeHorizonSection for "immediate" -- heading "Immediate Priorities", subhead "Actions to complete in the next 90 days"
      3. TimeHorizonSection for "strategic" -- heading "Strategic Initiatives", subhead "Longer-horizon programs (3-12 months)"
      4. TimeHorizonSection for "ongoing" -- heading "Ongoing Practices" (no subhead, just recurring cadence labels)
      5. ProgressDashboard section
    - Each section only renders if it has items (no empty section headers)

    Create src/components/action-plan/ExecutiveSummary.tsx ("use client"):
    - Hero surface card per UI-SPEC (hero-surface class pattern from advisor pages)
    - Heading "Where you stand today" per UI-SPEC (text-xl font-semibold font-display)
    - Overall readiness score: display the average urgencyScore across all included items as a large display number. Use font-display text-4xl for the score.
    - Top priorities list: show top 3 items by urgency (highest urgency first), name + status badge
    - Recently completed count: "{N} actions completed"
    - Upcoming review dates: if any items have deferredRevisitDate, show "Next review: {date}"

    Create src/components/action-plan/TimeHorizonSection.tsx ("use client"):
    - Accept horizon label, subhead, and array of action items
    - Render section heading with text-xl font-semibold font-display, subhead with text-sm text-muted-foreground
    - Render ActionCard list for items in this horizon
    - For "ongoing" horizon: each card shows a recurring cadence label if available (from timeframe field)
    - Uses Collapsible (shadcn) for strategic initiatives per UI-SPEC
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/app/\(protected\)/dashboard/action-plan/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Route page exists at src/app/(protected)/dashboard/action-plan/page.tsx
    - Route uses auth() session check and redirects ADVISOR/ADMIN roles
    - Route calls getClientActionPlan() for data
    - Empty state uses exact UI-SPEC copywriting: "Your action plan is being prepared"
    - Error state uses Alert destructive with exact UI-SPEC copy
    - StrategicActionPlan.tsx renders sections in order: Executive Summary, Immediate Priorities, Strategic Initiatives, Ongoing Practices, Progress Dashboard
    - Sections use 2xl (48px) vertical spacing
    - Section headings use font-display with exact UI-SPEC copy
    - ExecutiveSummary.tsx displays readiness score, top 3 priorities, completed count
    - TimeHorizonSection.tsx renders items within the given horizon
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Client Strategic Action Plan page renders with executive summary, three time-horizon sections, and proper empty/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Build action card with reasoning chain, status selector, and progress dashboard</name>
  <files>
    src/components/action-plan/ActionCard.tsx
    src/components/action-plan/ProgressDashboard.tsx
  </files>
  <read_first>
    src/components/assessment/FacilitatedRecommendations.tsx (card layout pattern with detail grid, badges, expandable content)
    .planning/phases/22-recommendation-experience/22-UI-SPEC.md (Client Action Tracking interaction contracts, Task status labels, Accessibility requirements for progress bars)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-19 -- full reasoning chain, D-21 -- dual status tracks, D-22 -- validation configurable)
  </read_first>
  <action>
    Create src/components/action-plan/ActionCard.tsx ("use client"):
    - Accept a single action item (GuidancePackageItem subset with composed solution data)
    - Card layout per UI-SPEC:
      * Title: recommendation name (text-xl font-semibold)
      * Status badge: task status using Select component (client-managed, per D-21). Options: "Not Started", "In Progress", "Waiting on Someone Else", "Ready for Review", "Completed" (exact labels from UI-SPEC)
      * Validation status badge (read-only, advisor-managed per D-21): show as Badge. PENDING_REVIEW = "Pending Review" (outline), VERIFIED = "Verified" (success), NEEDS_FOLLOWUP = "Needs Follow-up" (warning). Only show if requiresValidation is true.
      * Owner role badge(s): display responsibleRoles as Badge variant="outline" chips
      * Assignee display: if assignees present, show names below role badges

    Reasoning chain per D-19 (each as a labeled section):
      * "Why this was recommended": mergedEvidence / triggerReason formatted as readable text. Use Collapsible for expand/collapse if text exceeds 2 lines (text-sm, line-clamp-2 with expand)
      * "Expected Benefit": expectedOutcome field (text-sm)
      * "Supporting Insights": assessment source references (which assessments triggered this)
      * "Implementation Guidance": composed playbook steps rendered as numbered list. Each step shows title + description + estimated duration. Collapsible via shadcn Collapsible.
      * "Success Criteria": derived from the recommendation description + expected outcome

    Task status Select: on change, call updateTaskStatus server action with immediate optimistic UI update. Use useTransition for pending state.

    "Mark Complete" shortcut Button variant="outline" per UI-SPEC: sets taskStatus to COMPLETED. If requiresValidation is true, show a small note "Your advisor will verify this completion."

    Detail grid (shadcn Card section): estimated cost, timeframe, provider -- same layout as FacilitatedRecommendations.tsx detail grid pattern with CircleDollarSign, Clock, Building2 icons.

    Client experience per D-23: composed result is seamless guidance without layer labels. No Platform/Enterprise/Advisor badges on the client view (those are advisor-only). The client sees unified content.

    Create src/components/action-plan/ProgressDashboard.tsx ("use client"):
    - Accept all action items
    - Section heading "Your Progress" (text-xl font-semibold font-display) per UI-SPEC
    - Overall completion: calculate percentage of COMPLETED items out of total. Display as large percentage (text-3xl font-semibold) with Progress bar (shadcn).
    - Per-recommendation progress bars: for each active recommendation (INCLUDED or IN_PROGRESS), show a Progress bar with the recommendation name. Progress = percentage of milestones completed (if milestones exist) or binary (0% for not started, 50% for in progress, 100% for completed).
    - Progress bars use role="progressbar" with aria-valuenow, aria-valuemin="0", aria-valuemax="100" per UI-SPEC accessibility requirements.
    - If no active recommendations: don't render this section.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/components/action-plan/ActionCard.tsx src/components/action-plan/ProgressDashboard.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ActionCard.tsx renders title, task status Select, validation status Badge (conditional), role badges, assignees
    - ActionCard.tsx displays full reasoning chain per D-19: why recommended, expected benefit, supporting insights, implementation guidance, success criteria
    - Task status Select uses exact labels from UI-SPEC ("Not Started", "In Progress", "Waiting on Someone Else", "Ready for Review", "Completed")
    - Task status change calls updateTaskStatus server action
    - "Mark Complete" button exists per UI-SPEC
    - Client view does NOT show layer attribution badges (D-23 simplified view)
    - Implementation guidance is collapsible
    - ProgressDashboard.tsx shows overall completion percentage with Progress bar
    - ProgressDashboard.tsx shows per-recommendation progress bars
    - Progress bars have role="progressbar" with aria-valuenow, aria-valuemin, aria-valuemax
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Action cards display full reasoning chains and status selectors. Progress dashboard shows completion metrics with accessible progress bars.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Client Strategic Action Plan page at /dashboard/action-plan with:
    - Executive summary showing readiness score and top priorities
    - Three time-horizon sections (Immediate Priorities, Strategic Initiatives, Ongoing Practices)
    - Action cards with full reasoning chain (why, expected benefit, insights, implementation, success criteria)
    - Task status selector (client-managed: Not Started / In Progress / Waiting / Ready for Review / Completed)
    - Validation status badge (read-only, advisor-managed)
    - Progress dashboard with overall completion and per-recommendation progress bars
    - No layer attribution labels (client sees unified guidance)
  </what-built>
  <how-to-verify>
    1. Ensure advisor has included some recommendations for client@test.com (via /advisor/clients/[clientId]/guidance)
    2. Sign in as client@test.com via magic link
    3. Navigate to /dashboard/action-plan
    4. Verify: page title is "Your Strategic Action Plan"
    5. Verify: Executive Summary shows readiness score, top priorities
    6. Verify: "Immediate Priorities" section shows 0-90 day actions
    7. Click on an action card -- verify reasoning chain: "Why this was recommended", "Expected Benefit", implementation guidance
    8. Change task status via Select dropdown -- verify optimistic update
    9. Click "Mark Complete" -- verify status changes to Completed
    10. Verify: Progress Dashboard shows completion percentage and progress bars
    11. Verify: NO "Platform" / "Enterprise" / "Advisor" layer labels visible (client simplified view)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser -> action plan data | Client must only see their own action plan, hidden items excluded |
| client -> task status | Client can only change task status, never validation status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Information Disclosure | action-plan route | mitigate | getClientActionPlan filters by session.user.id, excludes hiddenFromClient=true items |
| T-22-15 | Elevation of Privilege | ActionCard.tsx | mitigate | Only taskStatus mutations exposed to client; validationStatus is read-only badge. Server action validates session ownership |
| T-22-16 | Information Disclosure | ActionCard.tsx | mitigate | Layer attribution not shown to clients (D-23); composed view only |
</threat_model>

<verification>
- TypeScript compilation passes for all action plan components
- Checkpoint: human verification of full client experience
</verification>

<success_criteria>
Client sees a structured Strategic Action Plan with executive summary, time-horizon sections, full reasoning chains on each action, task status controls, and progress tracking. The experience feels like following a long-term strategic roadmap.
</success_criteria>

<output>
Create `.planning/phases/22-recommendation-experience/22-06-SUMMARY.md` when done
</output>
