---
phase: 22-recommendation-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/recommendations/types.ts
  - src/lib/asset-catalog/types.ts
  - src/lib/asset-catalog/inheritance-engine.ts
  - src/lib/recommendations/override-policy.ts
  - src/lib/asset-catalog/inheritance-engine.test.ts
  - src/lib/recommendations/override-policy.test.ts
autonomous: true
requirements: [LIFECYCLE-01]

must_haves:
  truths:
    - "RecommendationStatus enum includes GENERATED, INCLUDED, DEFERRED, IN_PROGRESS alongside existing values"
    - "AssessmentRecommendation model has taskStatus, validationStatus, advisorPriority, hiddenFromClient, defer fields, responsibleRoles, assignees columns"
    - "EnterpriseSolutionCustomization has isRequired, priorityAdjustment, complianceDisclosures, customGuidance, internalLinks columns"
    - "Generic inheritance engine composes assets with override policy enforcement"
    - "Override policy correctly enforces PROTECTED/CONFIGURABLE/ADDITION tiers"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Extended enums and columns for recommendation lifecycle"
      contains: "GENERATED"
    - path: "src/lib/asset-catalog/inheritance-engine.ts"
      provides: "Generic asset composition with policy enforcement"
      exports: ["composeAsset"]
    - path: "src/lib/recommendations/override-policy.ts"
      provides: "Recommendation-specific override policy definitions"
      exports: ["RECOMMENDATION_FIELD_POLICIES", "validateOverlayFields"]
    - path: "src/lib/asset-catalog/types.ts"
      provides: "Asset-agnostic types for inheritance engine"
      exports: ["AssetLayer", "OverrideTier", "FieldOverridePolicy"]
  key_links:
    - from: "src/lib/recommendations/override-policy.ts"
      to: "src/lib/asset-catalog/inheritance-engine.ts"
      via: "import composeAsset"
      pattern: "import.*composeAsset.*inheritance-engine"
---

<objective>
Evolve the Prisma schema to support the new recommendation lifecycle (D-07, D-08, D-20, D-21, D-22) and build the generic Platform Asset Catalog inheritance engine (D-09, D-10, D-11) with override policy enforcement.

Purpose: Establish the data model foundation and generic composition engine that all subsequent plans depend on. Without this, no guidance actions, no UI surfaces, and no lifecycle transitions can be built.

Output: Extended Prisma schema with new enums and columns, generic inheritance engine with tests, recommendation-specific override policy with tests, shared types.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recommendation-experience/22-CONTEXT.md
@.planning/phases/22-recommendation-experience/22-RESEARCH.md
@.planning/phases/22-recommendation-experience/22-PATTERNS.md

<interfaces>
From prisma/schema.prisma (current RecommendationStatus enum, line 1470):
```
enum RecommendationStatus {
  PENDING
  REVIEWED
  ACCEPTED
  DECLINED
  COMPLETED
}
```

From src/lib/recommendations/compose-solution.ts:
```typescript
export type ComposedSolution = {
  serviceId: string;
  name: string;
  description: string;
  shortDescription: string | null;
  category: string;
  icon: string | null;
  expectedOutcome: string | null;
  tags: string[];
  estimatedCost: string | null;
  timeframe: string | null;
  provider: string | null;
  externalUrl: string | null;
  prerequisites: string[];
  playbook: ComposedPlaybookStep[];
  notes: string[];
  sourceLayer: SourceLayerSummary;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema with new enums and columns</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    prisma/schema.prisma (lines 1470-1978: RecommendationStatus enum, AssessmentRecommendation model, EnterpriseSolutionCustomization model, AdvisorSolutionCustomization model)
    .planning/phases/22-recommendation-experience/22-RESEARCH.md (Schema Evolution Summary section)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-07, D-08, D-14, D-20, D-21, D-22)
  </read_first>
  <action>
    Add new enum values to RecommendationStatus: GENERATED, INCLUDED, DEFERRED, IN_PROGRESS. Keep existing PENDING, REVIEWED, ACCEPTED, DECLINED, COMPLETED for backward compatibility. The migration will handle data mapping later; this step only adds the enum values so Prisma types are available.

    Create three new enums per D-21:
    - TaskStatus: NOT_STARTED, IN_PROGRESS, WAITING, READY_FOR_REVIEW, COMPLETED
    - ValidationStatus: PENDING_REVIEW, VERIFIED, NEEDS_FOLLOWUP
    - AdvisorPriority: HIGH, MEDIUM, LOW (per D-06)

    Add new columns to AssessmentRecommendation model per D-05/D-06/D-08/D-20/D-21/D-22:
    - taskStatus TaskStatus @default(NOT_STARTED) -- client-managed (D-21)
    - validationStatus ValidationStatus @default(PENDING_REVIEW) -- advisor-managed (D-21)
    - requiresValidation Boolean @default(false) -- platform/enterprise configurable (D-22)
    - advisorPriority AdvisorPriority? -- advisor override of urgencyScore (D-06)
    - hiddenFromClient Boolean @default(false) -- D-05 "Hide from Client"
    - deferredReason String? @db.Text -- D-08
    - deferredRevisitDate DateTime? -- D-08
    - deferredTriggerEvent String? @db.Text -- D-08
    - responsibleRoles String[] @default([]) -- D-20 platform-defined role list
    - assignees Json? -- D-20 freeform assignee list [{name, role, org}]
    - timeHorizon String? @db.VarChar(20) -- for SAP grouping: "immediate", "strategic", "ongoing"

    Add new columns to EnterpriseSolutionCustomization per D-14:
    - isRequired Boolean @default(false) @map("is_required")
    - priorityAdjustment Int? @map("priority_adjustment")
    - complianceDisclosures String? @db.Text @map("compliance_disclosures")
    - customGuidance String? @db.Text @map("custom_guidance")
    - internalLinks Json? @map("internal_links") -- shape: [{url, label}]

    Add overridePolicies Json? column to ServiceRecommendation model to store per-field override policy declarations per D-09/D-11. This is the generic asset catalog foundation -- recommendations are the first consumer.

    Add an index on AssessmentRecommendation for taskStatus and hiddenFromClient for efficient client-facing queries.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx prisma validate 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `enum TaskStatus` with NOT_STARTED, IN_PROGRESS, WAITING, READY_FOR_REVIEW, COMPLETED
    - prisma/schema.prisma contains `enum ValidationStatus` with PENDING_REVIEW, VERIFIED, NEEDS_FOLLOWUP
    - prisma/schema.prisma contains `enum AdvisorPriority` with HIGH, MEDIUM, LOW
    - RecommendationStatus enum contains GENERATED, INCLUDED, DEFERRED, IN_PROGRESS (alongside existing values)
    - AssessmentRecommendation model contains taskStatus, validationStatus, requiresValidation, advisorPriority, hiddenFromClient, deferredReason, deferredRevisitDate, deferredTriggerEvent, responsibleRoles, assignees, timeHorizon columns
    - EnterpriseSolutionCustomization model contains isRequired, priorityAdjustment, complianceDisclosures, customGuidance, internalLinks columns
    - ServiceRecommendation model contains overridePolicies Json? column
    - `npx prisma validate` exits 0
  </acceptance_criteria>
  <done>All new enums and columns are in the schema and Prisma validation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Schema push and type generation</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    prisma/schema.prisma (verify the changes from Task 1 are present)
  </read_first>
  <action>
    Run npx prisma db push to apply schema changes to the database. If destructive changes are detected (unlikely since all additions are new columns with defaults or nullable), use --accept-data-loss flag. Then run npx prisma generate to regenerate the Prisma client with the new types.

    After generation, verify that TypeScript can import the new enum types by checking the generated Prisma client output includes TaskStatus, ValidationStatus, AdvisorPriority, and the extended RecommendationStatus.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx prisma db push 2>&1 | tail -5 && npx prisma generate 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma db push completes without error
    - npx prisma generate completes without error
    - node_modules/.prisma/client/index.d.ts contains TaskStatus, ValidationStatus, AdvisorPriority exports
  </acceptance_criteria>
  <done>Database schema is in sync with Prisma schema, and generated client includes all new types.</done>
</task>

<task type="auto">
  <name>Task 3: Build generic asset catalog inheritance engine, override policy, and shared types</name>
  <files>
    src/lib/asset-catalog/types.ts
    src/lib/asset-catalog/inheritance-engine.ts
    src/lib/asset-catalog/inheritance-engine.test.ts
    src/lib/recommendations/override-policy.ts
    src/lib/recommendations/override-policy.test.ts
    src/lib/recommendations/types.ts
  </files>
  <read_first>
    src/lib/recommendations/compose-solution.ts (entire file -- the pattern to generalize)
    src/lib/recommendations/compose-solution.test.ts (test pattern to follow)
    src/lib/recommendations/types.ts (existing types to extend)
    .planning/phases/22-recommendation-experience/22-RESEARCH.md (Pattern 3: Override Policy, Pattern 4: Generic Asset Catalog)
    .planning/phases/22-recommendation-experience/22-PATTERNS.md (inheritance-engine.ts and override-policy.ts analog sections)
    .planning/phases/22-recommendation-experience/22-CONTEXT.md (D-09, D-10, D-11, D-12)
  </read_first>
  <action>
    Create src/lib/asset-catalog/types.ts with generic types per D-09/D-10:
    - OverrideTier union: "PROTECTED" | "CONFIGURABLE" | "ADDITION"
    - FieldOverridePolicy type: { field: string; tier: OverrideTier }
    - AssetLayer generic type: { platform: T; enterprise?: Partial<T> | null; advisor?: Partial<T> | null }
    - ComposedAsset generic type: T & { sourceAttribution: Record<string, "PLATFORM" | "ENTERPRISE" | "ADVISOR"> }

    Create src/lib/asset-catalog/inheritance-engine.ts with composeAsset function:
    - Accept AssetLayer<T> and FieldOverridePolicy[] parameters
    - For PROTECTED fields: always use platform value, ignore overlay values
    - For CONFIGURABLE fields: last-writer-wins (advisor > enterprise > platform)
    - For ADDITION fields: concatenate arrays from all layers
    - Build sourceAttribution record tracking which layer provided each field's value
    - Export composeAsset and a validateOverlayPayload helper that rejects writes to PROTECTED fields
    - This file does NOT import "server-only" -- it is pure logic, usable in tests

    Create src/lib/asset-catalog/inheritance-engine.test.ts:
    - Use Vitest describe/it/expect pattern from compose-solution.test.ts
    - Factory function makeAsset with sensible defaults
    - Test cases: platform-only (no overlays), enterprise override on CONFIGURABLE field, advisor override wins over enterprise, PROTECTED field cannot be overridden, ADDITION fields concatenate, validateOverlayPayload rejects PROTECTED writes, inactive layers ignored

    Create src/lib/recommendations/override-policy.ts:
    - Define RECOMMENDATION_FIELD_POLICIES mapping per D-11:
      PROTECTED: insight (name), whyItMatters (description), expectedOutcome, tags (evidence)
      CONFIGURABLE: estimatedCost, timeframe, provider, externalUrl, playbook
      ADDITION: notes, prerequisites
    - Export validateOverlayFields(fields: string[]): { allowed: string[]; rejected: string[] } that checks field names against the policy map
    - Export getRecommendationPolicies() returning FieldOverridePolicy[]

    Create src/lib/recommendations/override-policy.test.ts:
    - Test validateOverlayFields rejects PROTECTED field names (name, description, expectedOutcome)
    - Test validateOverlayFields allows CONFIGURABLE fields (estimatedCost, timeframe, provider)
    - Test getRecommendationPolicies returns correct tier for each known field

    Extend src/lib/recommendations/types.ts with new types needed for guidance package:
    - GuidancePackageItem: extends PortfolioRecommendationItem with advisorPriority, hiddenFromClient, taskStatus, validationStatus, requiresValidation, deferredReason, deferredRevisitDate, deferredTriggerEvent, responsibleRoles, assignees, timeHorizon, assessmentSources (string[]), mergedEvidence (unknown[])
    - GuidancePackage: { clientId: string; clientName: string; items: GuidancePackageItem[]; summary: GuidancePackageSummary }
    - GuidancePackageSummary: { totalItems: number; includedCount: number; deferredCount: number; completedCount: number; inProgressCount: number; hiddenCount: number }
    - DeferInput: { recommendationId: string; reason: string; revisitDate?: string; triggerEvent?: string; notes?: string }
    - BulkActionInput: { recommendationIds: string[]; action: "INCLUDE" | "DEFER"; deferReason?: string; deferRevisitDate?: string }
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx vitest run src/lib/asset-catalog/inheritance-engine.test.ts src/lib/recommendations/override-policy.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/asset-catalog/types.ts exports OverrideTier, FieldOverridePolicy, AssetLayer, ComposedAsset
    - src/lib/asset-catalog/inheritance-engine.ts exports composeAsset and validateOverlayPayload
    - inheritance-engine.test.ts has at least 6 test cases covering platform-only, CONFIGURABLE override, PROTECTED enforcement, ADDITION concatenation
    - src/lib/recommendations/override-policy.ts exports RECOMMENDATION_FIELD_POLICIES, validateOverlayFields, getRecommendationPolicies
    - override-policy.test.ts has at least 3 test cases for field validation
    - src/lib/recommendations/types.ts exports GuidancePackageItem, GuidancePackage, GuidancePackageSummary, DeferInput, BulkActionInput
    - All tests pass
  </acceptance_criteria>
  <done>Generic inheritance engine enforces three-tier override policy, recommendation-specific policies are defined and tested, and guidance package types are available for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema evolution | New columns with defaults; no untrusted input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | override-policy.ts | mitigate | validateOverlayPayload rejects writes to PROTECTED fields server-side before any Prisma mutation |
| T-22-02 | Tampering | inheritance-engine.ts | mitigate | composeAsset enforces policy at composition time; PROTECTED fields always sourced from platform layer |
</threat_model>

<verification>
- npx prisma validate passes
- npx prisma db push completes
- npx prisma generate completes
- All inheritance engine tests pass
- All override policy tests pass
</verification>

<success_criteria>
Database schema supports the full recommendation lifecycle with new enums and columns. Generic asset inheritance engine composes Platform -> Enterprise -> Advisor layers with three-tier override policy enforcement. All foundation types are exported for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/22-recommendation-experience/22-01-SUMMARY.md` when done
</output>
