---
phase: 24-continuous-risk-improvement
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/lib/actions/reassessment-actions.ts
  - src/lib/actions/cadence-actions.ts
  - src/app/api/cron/review-cadence/route.ts
  - src/lib/engagement/activity-feed.ts
autonomous: true
requirements:
  - LIFECYCLE-03
must_haves:
  truths:
    - "Server actions for reassessment creation are guarded by role and ownership checks"
    - "Cadence server actions enforce advisor-client assignment"
    - "Cron route authenticates with CRON_SECRET and processes overdue/due-soon cadences"
    - "Activity feed handles nullable assessmentRecommendationId and returns intelligence events"
  artifacts:
    - path: "src/lib/actions/reassessment-actions.ts"
      provides: "startReassessmentAction, getScoreDeltasAction, getTargetedQuestionCountAction"
      exports: ["startReassessmentAction", "getScoreDeltasAction", "getTargetedQuestionCountAction"]
    - path: "src/lib/actions/cadence-actions.ts"
      provides: "setCadenceAction, overrideCadenceAction, getCadenceAction"
      exports: ["setCadenceAction", "overrideCadenceAction", "getCadenceAction"]
    - path: "src/app/api/cron/review-cadence/route.ts"
      provides: "GET handler for daily cadence cron"
      exports: ["GET"]
    - path: "src/lib/engagement/activity-feed.ts"
      provides: "Updated getClientActivityFeed with intelligence event support"
      exports: ["getClientActivityFeed"]
  key_links:
    - from: "src/lib/actions/reassessment-actions.ts"
      to: "src/lib/assessment/reassessment.ts"
      via: "calls createReassessment after auth + validation"
      pattern: "createReassessment"
    - from: "src/lib/actions/cadence-actions.ts"
      to: "src/lib/cadence/review-cadence.ts"
      via: "calls cadence CRUD after auth + assignment check"
      pattern: "createOrUpdateCadence"
    - from: "src/app/api/cron/review-cadence/route.ts"
      to: "src/lib/cadence/review-cadence.ts"
      via: "calls getOverdueCadences and getDueSoonCadences"
      pattern: "getOverdueCadences|getDueSoonCadences"
---

<objective>
Server actions, cron route, and activity feed evolution. Wire the business logic from Plans 01-02 into authenticated, validated server actions and a cron-driven cadence processor.

Purpose: Provide the API layer that the UI (Plan 04) will call. Per D-07 (score deltas visible to both client and advisor), D-08 (advisor cadence override), D-10 (Review Cadence Engine manages lifecycle), and D-11 (evolve activity feed).

Output: Reassessment and cadence server actions with Zod validation and role guards, cron route with CRON_SECRET auth, updated activity feed query supporting intelligence events.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-continuous-risk-improvement/24-CONTEXT.md
@.planning/phases/24-continuous-risk-improvement/24-RESEARCH.md
@.planning/phases/24-continuous-risk-improvement/PATTERNS.md
@.planning/phases/24-continuous-risk-improvement/24-01-SUMMARY.md
@.planning/phases/24-continuous-risk-improvement/24-02-SUMMARY.md

@src/lib/actions/engagement-actions.ts (server action pattern: "use server", requireAdvisorRole, Zod, ActionResult, fail/ok helpers, ownership verification, revalidatePath)
@src/app/api/cron/advisory-outreach-reminder/route.ts (cron auth pattern: CRON_SECRET, timingSafeEqual, Bearer header)
@src/lib/engagement/activity-feed.ts (existing feed query -- must update for nullable FK)

<interfaces>
From Plan 01 outputs:
- src/lib/assessment/reassessment-types.ts: ReassessmentType, PillarDelta, ReassessmentInput
- src/lib/assessment/reassessment.ts: createReassessment(input), getReassessmentChain(assessmentId), getLatestCompletedAssessment(userId)
- src/lib/analytics/score-delta.ts: computePillarDeltas(prev, curr, recs), getScoreDeltasForAssessment(assessmentId, previousAssessmentId)
- src/lib/assessment/targeted-followup.ts: getTargetedFollowupQuestions(assessmentId), getTargetedQuestionCount(assessmentId)

From Plan 02 outputs:
- src/lib/engagement/intelligence-events.ts: INTELLIGENCE_ACTIONS, logIntelligenceEvent(params), CLIENT_VISIBLE_INTELLIGENCE_ACTIONS
- src/lib/cadence/review-cadence.ts: createOrUpdateCadence(input), getCadenceForClient(clientId, advisorProfileId), computeNextDueDate(completedAt, freq), getOverdueCadences(), getDueSoonCadences(threshold?)
- src/lib/cadence/system-triggers.ts: checkSystemReassessmentTriggers(clientId, assessmentId)
- src/lib/cadence/cadence-types.ts: CadenceStatus, CadenceInfo
- src/lib/engagement/feature-flags.ts: isCadenceEngineEnabled(advisorProfileId)

From existing codebase:
- src/lib/actions/engagement-actions.ts pattern:
  "use server"; requireAdvisorRole(); Zod parse; ownership check; service call; revalidatePath; return ok/fail
- src/lib/engagement/activity-feed.ts:
  getClientActivityFeed({ clientId, limit, offset, role }): Promise<ActivityFeedItem[]>
  Currently filters via assessmentRecommendation.assessment.userId -- must update for nullable assessmentRecommendationId
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reassessment and cadence server actions</name>
  <files>src/lib/actions/reassessment-actions.ts, src/lib/actions/cadence-actions.ts</files>
  <action>
Create `src/lib/actions/reassessment-actions.ts` following engagement-actions.ts pattern exactly:
- "use server" directive at top
- Import requireAdvisorRole from @/lib/advisor/auth, auth from @/lib/auth, prisma from @/lib/db, logSafeError/safeErrorMessage from @/lib/log-safe-error, revalidatePath from next/cache, z from zod
- ActionResult type + fail/ok helpers (same as engagement-actions.ts)
- Ownership verification: `verifyAssessmentAccess(assessmentId: string, userId: string, role: string)` -- For ADVISOR: check ClientAdvisorAssignment exists between advisor and assessment's userId. For USER (client): check assessment.userId === userId. Per D-07, both roles can view deltas.

Zod schemas:
- `startReassessmentSchema = z.object({ previousAssessmentId: z.string().cuid(), type: z.enum(["full", "pillar", "targeted"]), includedPillars: z.array(z.string()).optional() })` (per D-01)

Server actions:
- `startReassessmentAction(input)`: requireAdvisorRole (advisor initiates), parse with Zod, verify advisor owns the client's assessment, load previous assessment, call createReassessment from Plan 01, log INTELLIGENCE_ACTIONS.REASSESSMENT_TRIGGERED via logIntelligenceEvent with assessmentId, revalidatePath("/advisor"), return ok with new assessment id. For targeted type, call getTargetedFollowupQuestions to get questionIds and pass to createReassessment.
- `getScoreDeltasAction(assessmentId: string)`: Use auth() to get session. Verify access (advisor OR client per D-07). Load assessment, check it has previousAssessmentId. Call getScoreDeltasForAssessment. Return ok with PillarDelta array.
- `getTargetedQuestionCountAction(assessmentId: string)`: Auth + verify access. Call getTargetedQuestionCount. Return ok with count number.
- `getReassessmentHistoryAction(userId: string)`: Auth + verify access. Call getReassessmentChain for the latest assessment. Return ok with chain entries.

Create `src/lib/actions/cadence-actions.ts` following same pattern:
- `setCadenceSchema = z.object({ clientId: z.string().cuid(), frequency: z.enum(["QUARTERLY", "SEMI_ANNUAL", "ANNUAL"]) })`
- `overrideCadenceSchema = z.object({ clientId: z.string().cuid(), frequency: z.enum(["QUARTERLY", "SEMI_ANNUAL", "ANNUAL"]) })`

Server actions:
- `setCadenceAction(input)`: requireAdvisorRole, Zod parse, verify advisor-client assignment, check isCadenceEngineEnabled, call createOrUpdateCadence with isOverridden=false, log INTELLIGENCE_ACTIONS.CADENCE_CHANGED via logIntelligenceEvent, revalidatePath, return ok with CadenceInfo.
- `overrideCadenceAction(input)`: Same but isOverridden=true (per D-08 advisor override). Log CADENCE_CHANGED event.
- `getCadenceAction(clientId: string)`: requireAdvisorRole, verify assignment, call getCadenceForClient, return ok with CadenceInfo or null.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/lib/actions/reassessment-actions.ts src/lib/actions/cadence-actions.ts 2>&1 | head -20 || echo "Type check complete"</automated>
  </verify>
  <done>Reassessment server actions handle start, score delta retrieval, targeted question count, and history with proper auth guards. Cadence server actions handle set, override, and get with advisor-client assignment verification. Both log intelligence events. Zod validation on all inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Cron route and activity feed evolution</name>
  <files>src/app/api/cron/review-cadence/route.ts, src/lib/engagement/activity-feed.ts</files>
  <action>
Create `src/app/api/cron/review-cadence/route.ts` following advisory-outreach-reminder/route.ts pattern exactly:
- Import timingSafeEqual from crypto
- GET handler with CRON_SECRET Bearer token auth (same pattern: check header, extract token, length check, timingSafeEqual)
- On auth success: call getOverdueCadences() to find overdue clients, call getDueSoonCadences() for approaching due dates
- For each overdue cadence: log INTELLIGENCE_ACTIONS.CADENCE_OVERDUE via logIntelligenceEvent (use lastAssessmentId as assessmentId if available), update ReviewCadence.lastReminderSentAt to now (per Pitfall 5 dedup)
- For each due-soon cadence: log INTELLIGENCE_ACTIONS.CADENCE_DUE_APPROACHING, update lastReminderSentAt
- Call checkSystemReassessmentTriggers for each cadence's client/assessment. If shouldRecommend, update ReviewCadence with systemRecommended=true and systemRecommendationReason, log CADENCE_SYSTEM_RECOMMENDED
- Return JSON with success, overdue count, due-soon count, system-recommended count, processingTimeMs, timestamp (per cron pattern)
- Wrap everything in try/catch; return 500 with error message on failure

Update `src/lib/engagement/activity-feed.ts`:
- Update the Prisma query to handle nullable assessmentRecommendationId. The where clause currently filters via `assessmentRecommendation.assessment.userId`. For intelligence events with assessmentId but no assessmentRecommendationId, add an OR condition: `OR: [{ assessmentRecommendation: { assessment: { userId: clientId, status: "COMPLETED" } } }, { assessmentId: { not: null }, assessment: { userId: clientId } }]`
- Update CLIENT_VISIBLE_ACTIONS array: merge in CLIENT_VISIBLE_INTELLIGENCE_ACTIONS from intelligence-events.ts
- Update the ActivityFeedItem mapping: when assessmentRecommendationId is null (intelligence event), set recommendationName to a descriptive label based on action type (e.g., "Assessment completed" for assessment_completed, "Score change" for pillar_score_delta). Add a new optional field `eventType: "recommendation" | "intelligence"` to ActivityFeedItem to help the UI distinguish.
- Keep backward compatibility: all existing recommendation-based activity items continue to work unchanged.
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npx tsc --noEmit src/app/api/cron/review-cadence/route.ts src/lib/engagement/activity-feed.ts 2>&1 | head -20 || echo "Type check complete"</automated>
  </verify>
  <done>Cron route authenticates with CRON_SECRET and processes overdue/due-soon cadences with dedup. Activity feed query handles both recommendation-based and intelligence events via OR condition. CLIENT_VISIBLE_ACTIONS includes intelligence event types. ActivityFeedItem has eventType discriminator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client/Advisor -> Server Actions | Untrusted input validated with Zod, auth via requireAdvisorRole/auth() |
| Cron scheduler -> review-cadence route | CRON_SECRET Bearer token auth |
| Activity feed -> client role | Role-filtered action visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Elevation of Privilege | startReassessmentAction | mitigate | requireAdvisorRole + verifyAssessmentAccess checks advisor-client assignment |
| T-24-08 | Information Disclosure | getScoreDeltasAction | mitigate | auth() + verifyAssessmentAccess ensures only authorized users see deltas (D-07: both client and advisor allowed) |
| T-24-09 | Tampering | cadence actions | mitigate | requireAdvisorRole + advisor-client assignment verification on all cadence mutations |
| T-24-10 | Spoofing | cron route | mitigate | CRON_SECRET + timingSafeEqual per established pattern |
| T-24-11 | Information Disclosure | activity feed | mitigate | CLIENT_VISIBLE_ACTIONS filtering restricts intelligence events visible to client role |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npm run build` compiles without errors
- Cron route returns 401 without valid CRON_SECRET
- Activity feed returns both recommendation and intelligence events
</verification>

<success_criteria>
- startReassessmentAction creates linked assessment after auth + Zod validation
- getScoreDeltasAction returns per-pillar deltas accessible to both advisor and client
- Cadence actions enforce advisor-client assignment
- Cron route processes overdue cadences with dedup (lastReminderSentAt within 7 days skipped)
- Activity feed handles nullable assessmentRecommendationId via OR query
- All intelligence events logged via logIntelligenceEvent
</success_criteria>

<output>
Create `.planning/phases/24-continuous-risk-improvement/24-03-SUMMARY.md` when done
</output>
