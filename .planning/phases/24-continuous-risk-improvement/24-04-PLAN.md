---
phase: 24-continuous-risk-improvement
plan: 04
type: execute
wave: 3
depends_on:
  - 24-03
files_modified:
  - src/components/assessment/PillarDeltaPanel.tsx
  - src/components/assessment/ReassessmentDialog.tsx
  - src/components/engagement/IntelligenceTimeline.tsx
  - src/components/engagement/ReviewCadencePanel.tsx
autonomous: false
requirements:
  - LIFECYCLE-03
must_haves:
  truths:
    - "Client and advisor see per-pillar score deltas with attribution"
    - "Reassessment dialog offers three types with correct enabled/disabled states"
    - "Intelligence timeline renders tabbed activity feed with new event types"
    - "Review cadence panel shows status, due date, and override controls"
  artifacts:
    - path: "src/components/assessment/PillarDeltaPanel.tsx"
      provides: "Per-pillar delta comparison grid with attribution"
      exports: ["PillarDeltaPanel"]
    - path: "src/components/assessment/ReassessmentDialog.tsx"
      provides: "Three-option reassessment type selector dialog"
      exports: ["ReassessmentDialog"]
    - path: "src/components/engagement/IntelligenceTimeline.tsx"
      provides: "Tabbed intelligence timeline with event type filtering"
      exports: ["IntelligenceTimeline"]
    - path: "src/components/engagement/ReviewCadencePanel.tsx"
      provides: "Cadence status card with override controls"
      exports: ["ReviewCadencePanel"]
  key_links:
    - from: "src/components/assessment/PillarDeltaPanel.tsx"
      to: "getScoreDeltasAction"
      via: "server action call for delta data"
      pattern: "getScoreDeltasAction"
    - from: "src/components/assessment/ReassessmentDialog.tsx"
      to: "startReassessmentAction"
      via: "server action on Begin Reassessment click"
      pattern: "startReassessmentAction"
    - from: "src/components/engagement/IntelligenceTimeline.tsx"
      to: "getClientActivityFeed"
      via: "SSR data fetch with event type filtering"
      pattern: "getClientActivityFeed"
    - from: "src/components/engagement/ReviewCadencePanel.tsx"
      to: "overrideCadenceAction"
      via: "server action on cadence override"
      pattern: "overrideCadenceAction"
---

<objective>
Build all four UI surfaces from the UI-SPEC: Pillar Delta Comparison Panel, Reassessment Type Selector Dialog, Review Cadence Panel, and Intelligence Timeline. All components use existing shadcn/ui primitives.

Purpose: Per D-05 (per-pillar comparison with attribution), D-01 (three reassessment types), D-08 (advisor cadence override), and D-12 (intelligence event types in timeline). UI-SPEC provides exact layout, copywriting, color tokens, and interaction states.

Output: Four React components rendering correctly with loading, empty, error, and populated states. Human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-continuous-risk-improvement/24-CONTEXT.md
@.planning/phases/24-continuous-risk-improvement/24-UI-SPEC.md
@.planning/phases/24-continuous-risk-improvement/PATTERNS.md
@.planning/phases/24-continuous-risk-improvement/24-03-SUMMARY.md

@src/components/analytics/AssessmentComparisonView.tsx (side-by-side comparison layout analog)
@src/components/analytics/TrendIndicator.tsx (direction badge pattern)
@src/components/analytics/ChartContainer.tsx (Card + ResponsiveContainer pattern)

<interfaces>
From Plan 01 (types):
type ReassessmentType = "full" | "pillar" | "targeted";
type PillarDelta = {
  pillar: string; previousScore: number; currentScore: number;
  delta: number; direction: "improved" | "regressed" | "unchanged";
  previousRiskLevel: string; currentRiskLevel: string; attribution: string[];
};

From Plan 02 (cadence types):
type CadenceStatus = "on_track" | "due_soon" | "overdue" | "system_recommended";
type CadenceInfo = {
  id: string; clientId: string; frequency: CadenceFrequency;
  nextDueDate: Date; status: CadenceStatus; daysUntilDue: number;
  isOverridden: boolean; systemRecommended: boolean;
  systemRecommendationReason: string | null; lastAssessmentId: string | null;
};

From Plan 03 (server actions):
startReassessmentAction(input: { previousAssessmentId, type, includedPillars? }): Promise<ActionResult<{ id: string }>>
getScoreDeltasAction(assessmentId: string): Promise<ActionResult<PillarDelta[]>>
getTargetedQuestionCountAction(assessmentId: string): Promise<ActionResult<number>>
setCadenceAction(input: { clientId, frequency }): Promise<ActionResult<CadenceInfo>>
overrideCadenceAction(input: { clientId, frequency }): Promise<ActionResult<CadenceInfo>>
getCadenceAction(clientId: string): Promise<ActionResult<CadenceInfo | null>>

From Plan 03 (activity feed):
type ActivityFeedItem = {
  id: string; action: string; detail: unknown; createdAt: Date;
  actorId: string | null; recommendationName: string; recommendationId: string;
  eventType: "recommendation" | "intelligence";
};
getClientActivityFeed({ clientId, limit, offset, role }): Promise<ActivityFeedItem[]>

From Plan 02 (intelligence events):
INTELLIGENCE_ACTIONS: { ASSESSMENT_STARTED, ASSESSMENT_COMPLETED, SCORE_CALCULATED, ... }

shadcn/ui components (all installed):
Card, CardHeader, CardContent, Badge, Button, Dialog, DialogTrigger, DialogContent,
DialogHeader, DialogTitle, DialogDescription, Tabs, TabsList, TabsTrigger, TabsContent,
Progress, Select, SelectTrigger, SelectContent, SelectItem, SelectValue,
Skeleton, Separator, Tooltip, TooltipTrigger, TooltipContent, TooltipProvider
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PillarDeltaPanel and ReassessmentDialog components</name>
  <files>src/components/assessment/PillarDeltaPanel.tsx, src/components/assessment/ReassessmentDialog.tsx</files>
  <action>
Create `src/components/assessment/PillarDeltaPanel.tsx` (follow AssessmentComparisonView.tsx layout pattern):

Props: `{ deltas: PillarDelta[]; loading?: boolean; error?: string | null }`

Per UI-SPEC "Pillar Delta Comparison Panel":
- Responsive grid: `grid grid-cols-1 lg:grid-cols-2 gap-6`
- Each pillar renders a Card with `border-border/80 bg-card/92`
- Card content structure:
  - Pillar name as kicker: use `professional-kicker` utility class (12px, weight 600, letter-spacing 0.12em, uppercase)
  - Score row: "Previous score -> Current score" with delta chip
  - Delta chip: rounded-full, py-1 px-2, text-xs font-semibold
    - Positive: `bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300` with TrendingUp icon (lucide)
    - Negative: `bg-destructive/10 text-destructive` with TrendingDown icon
    - No change: `bg-muted text-muted-foreground` with Minus icon
  - Attribution section below score row:
    - Header: "Why it improved:" / "Why it regressed:" / "Why it's unchanged:" (per Copywriting Contract)
    - Bullet list of attribution strings, max 3 visible
    - If >3 attributions, show "Show more" link that expands to full list (use React state toggle)
  - Zero-state card (direction === "unchanged" and attribution is ["No new planning activity"]): centered text "No change -- No new planning activity" in card body (per D-06)

Loading state: Skeleton cards in same grid layout using Skeleton component with animate-pulse
Error state: Inline error card with text from Copywriting Contract ("Scores could not be loaded. Check your connection and try again.") and retry Button variant="ghost"
Empty state (no deltas array or empty array): Full-width dashed border card with heading "No previous assessment to compare" and body "Complete your first reassessment to see per-pillar improvement and track the impact of your planning work." (per Copywriting Contract)

Create `src/components/assessment/ReassessmentDialog.tsx`:

Props: `{ assessmentId: string; targetedQuestionCount: number; onReassessmentStarted?: (newAssessmentId: string) => void }`

Per UI-SPEC "Reassessment Type Selector":
- Trigger: Button with text "Start Reassessment" (client) or "Reassess Now" (advisor) -- accept `triggerLabel` prop defaulting to "Start Reassessment"
- Dialog with DialogTitle "Choose Reassessment Type"
- Three option cards in vertical list inside DialogContent:
  1. Full Assessment -- description: "Annual review or major life event"
  2. Domain / Pillar Reassessment -- description: "Retake a single pillar"
     - When selected, show inline Select component for pillar selection
  3. Targeted Follow-up -- description: "Re-ask questions linked to completed recommendations"
     - Show count badge: "{targetedQuestionCount} eligible questions"
     - If targetedQuestionCount === 0: card has opacity-50 cursor-not-allowed, wrapped in TooltipProvider + Tooltip with content "No completed recommendations with linked questions yet" (per UI-SPEC disabled state, Pitfall 4)
- Selected option styling: left border 2px solid accent + bg-accent/5
- React state: selectedType (ReassessmentType | null), selectedPillar (string | null for pillar type)
- CTA at bottom: Button "Begin Reassessment" (primary), disabled until type selected (and pillar selected for pillar type)
- Disclaimer below CTA: 12px text-muted-foreground "Previous assessment is preserved. This starts a new version." (per Copywriting Contract)
- On Begin click: call startReassessmentAction with { previousAssessmentId: assessmentId, type: selectedType, includedPillars: selectedPillar ? [selectedPillar] : undefined }. Handle loading state on button. On success, close dialog and call onReassessmentStarted. On error, show inline error toast.
- Include pillar list for the Select: import pillar options from the pillar registry or use a hardcoded array of canonical pillar IDs (governance, cyber-digital, identity, estate, insurance, physical-security, tax, liquidity, behavioral, reputational-social).
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PillarDeltaPanel renders per-pillar delta cards with attribution, loading/error/empty states per UI-SPEC. ReassessmentDialog offers three types with correct disabled state for targeted when count is 0. Pillar selector shown for pillar type. All copywriting matches contract.</done>
</task>

<task type="auto">
  <name>Task 2: IntelligenceTimeline and ReviewCadencePanel components</name>
  <files>src/components/engagement/IntelligenceTimeline.tsx, src/components/engagement/ReviewCadencePanel.tsx</files>
  <action>
Create `src/components/engagement/IntelligenceTimeline.tsx` (extends Phase 23 activity feed UI pattern):

Props: `{ items: ActivityFeedItem[]; loading?: boolean }`

Per UI-SPEC "Intelligence Timeline":
- Tabs component with filter tabs: "All" | "Assessments" | "Score Changes" | "Cadence" | "Recommendation Impact"
- Tab filtering logic maps INTELLIGENCE_ACTIONS to tab categories:
  - Assessments: assessment_started, assessment_completed, score_calculated, reassessment_triggered
  - Score Changes: pillar_score_delta, risk_level_transition
  - Cadence: cadence_due_approaching, cadence_overdue, cadence_changed, cadence_system_recommended
  - Recommendation Impact: recommendation_impact_measured, completion_milestone_reached
  - All existing recommendation actions (status_*, milestone_*) also appear in "All" tab
- Each event row layout:
  - Icon (16px lucide) + event summary text (14px) + relative timestamp (12px text-muted-foreground)
  - Icon mapping: FileText for assessment events, TrendingUp/TrendingDown for score changes, Calendar for cadence, CheckCircle2 for recommendation impact, Activity for recommendation lifecycle events
  - Summary text derived from action + detail JSON. Examples: "Assessment completed" (assessment_completed), "Governance score improved +22" (pillar_score_delta with detail.pillar and detail.delta), "Reassessment due in 14 days" (cadence_due_approaching)
  - Relative timestamp: use date-fns formatDistanceToNow
- Loading state: 5 Skeleton rows at 40px height (per UI-SPEC)
- Empty state per tab: centered muted text "No [Assessment/Score/Cadence/Recommendation] events yet." (per Copywriting Contract)

Create `src/components/engagement/ReviewCadencePanel.tsx`:

Props: `{ cadence: CadenceInfo | null; clientId: string; onCadenceUpdated?: () => void; loading?: boolean }`

Per UI-SPEC "Review Cadence Panel":
- Card component showing current cadence status
- Display: frequency label (Annual / Semi-annual / Quarterly), next due date (formatted with date-fns format), days until due
- Status Badge mapping:
  - on_track: variant="secondary" className="bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300" text "On track"
  - due_soon: variant="secondary" className using accent color, text "Due in {N} days" (per Copywriting Contract)
  - overdue: variant="destructive" text "Overdue -- {N} days past due" (per Copywriting Contract)
  - system_recommended: variant="secondary" with accent, text "AKILI Recommends" (per Copywriting Contract) with Button "View Recommendation"
- "Override Cadence" link that opens a Popover (not a full dialog) containing:
  - Select with options: Annual, Semi-annual, Quarterly
  - Save Button
  - On save: call overrideCadenceAction with { clientId, frequency: selected }
  - Per D-08: advisor override sets isOverridden=true
- Show "(Advisor override)" text next to frequency when isOverridden is true
- If cadence.systemRecommended: show the systemRecommendationReason text below the badge
- Loading state: Skeleton card
- Null cadence (no cadence set): show "No review cadence configured" with "Set Cadence" Button that opens the override popover
  </action>
  <verify>
    <automated>cd /Users/bwoodtalton/Projects/AkiliRisk && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>IntelligenceTimeline renders tabbed activity feed with icon mapping, summary text, relative timestamps, and per-tab empty states. ReviewCadencePanel shows cadence status with badge, due date, override popover, and system recommendation display. All copywriting matches UI-SPEC contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Four UI components for Phase 24 Continuous Risk Improvement: PillarDeltaPanel (per-pillar score delta cards with attribution), ReassessmentDialog (three-type selector with pillar picker and targeted count), IntelligenceTimeline (tabbed activity feed with intelligence events), ReviewCadencePanel (cadence status with override controls).</what-built>
  <how-to-verify>
    1. Run `npm run dev` and sign in as advisor (advisor@test.com / Testpass1)
    2. Navigate to any client detail view
    3. Verify PillarDeltaPanel renders (may show empty state "No previous assessment to compare" if no reassessment exists -- this is correct)
    4. Click "Start Reassessment" / "Reassess Now" button -- verify dialog opens with three options
    5. Verify "Targeted Follow-up" option shows count badge or disabled state with tooltip
    6. Verify selecting "Domain/Pillar Reassessment" shows inline pillar selector
    7. Check IntelligenceTimeline renders with tab filters (All, Assessments, Score Changes, Cadence, Recommendation Impact)
    8. Check ReviewCadencePanel shows cadence status or "No review cadence configured" setup state
    9. Verify "Override Cadence" link opens popover with frequency selector
    10. Check all loading/skeleton states render correctly (can test by throttling network in DevTools)
    11. Verify dark mode renders correctly for delta chips (emerald dark variants)
  </how-to-verify>
  <resume-signal>Type "approved" if all components render correctly, or describe any visual or functional issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Server Actions | Client-side components call server actions; all validation server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12 | Tampering | ReassessmentDialog | mitigate | Server action validates input with Zod; client-side pillar list is for UX only |
| T-24-13 | Information Disclosure | PillarDeltaPanel | mitigate | Data comes from server action which checks auth; component only renders what it receives |
</threat_model>

<verification>
- `npm run build` passes
- All four components render without runtime errors
- Loading, empty, error states work per UI-SPEC
- Copywriting matches UI-SPEC contract exactly
- Human verification confirms visual correctness
</verification>

<success_criteria>
- PillarDeltaPanel shows per-pillar delta cards with direction chips, attribution lists, and zero-state per D-05/D-06
- ReassessmentDialog offers three types per D-01 with correct disabled state for targeted (Pitfall 4)
- IntelligenceTimeline renders tabbed feed with icon mapping per D-12
- ReviewCadencePanel shows status badge, due date, override controls per D-08
- All states (loading, empty, error, populated) render per UI-SPEC
- Human verification passes
</success_criteria>

<output>
Create `.planning/phases/24-continuous-risk-improvement/24-04-SUMMARY.md` when done
</output>
