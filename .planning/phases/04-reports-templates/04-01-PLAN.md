---
phase: 04-reports-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/pdf/styles.ts
  - src/lib/pdf/components/ReportCover.tsx
  - src/lib/pdf/components/ExecutiveSummary.tsx
  - src/lib/pdf/components/CategoryBreakdown.tsx
  - src/lib/pdf/components/RecommendationsSection.tsx
  - src/lib/pdf/components/AssessmentReport.tsx
  - src/app/api/reports/[id]/pdf/route.tsx
autonomous: true

must_haves:
  truths:
    - "API endpoint generates valid PDF from assessment data"
    - "PDF contains executive summary with overall score and risk level"
    - "PDF contains category-by-category score breakdown"
    - "PDF contains specific recommendations for identified governance gaps"
    - "PDF has professional formatting with branded cover page"
  artifacts:
    - path: "src/lib/pdf/components/AssessmentReport.tsx"
      provides: "Complete PDF document composition"
      exports: ["AssessmentReport"]
    - path: "src/app/api/reports/[id]/pdf/route.tsx"
      provides: "PDF generation API endpoint"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/reports/[id]/pdf/route.tsx"
      to: "prisma.pillarScore"
      via: "database query for score data"
      pattern: "prisma\\.pillarScore\\.findUnique"
    - from: "src/app/api/reports/[id]/pdf/route.tsx"
      to: "src/lib/pdf/components/AssessmentReport.tsx"
      via: "renderToBuffer"
      pattern: "renderToBuffer.*AssessmentReport"
---

<objective>
Install @react-pdf/renderer and build server-side PDF report generation with professional formatting.

Purpose: Users need a downloadable PDF report with executive summary, score breakdown, and recommendations — the primary deliverable of the assessment.
Output: API endpoint that generates branded PDF reports from assessment data.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reports-templates/04-RESEARCH.md
@src/lib/assessment/types.ts
@src/lib/assessment/scoring.ts
@src/lib/assessment/questions.ts
@src/app/api/assessment/[id]/score/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create PDF component library</name>
  <files>
    package.json
    src/lib/pdf/styles.ts
    src/lib/pdf/components/ReportCover.tsx
    src/lib/pdf/components/ExecutiveSummary.tsx
    src/lib/pdf/components/CategoryBreakdown.tsx
    src/lib/pdf/components/RecommendationsSection.tsx
    src/lib/pdf/components/AssessmentReport.tsx
  </files>
  <action>
    1. Install: `npm install @react-pdf/renderer`

    2. Create `src/lib/pdf/styles.ts` with shared StyleSheet:
       - Professional fonts (Helvetica family built into @react-pdf/renderer)
       - Color palette: dark navy (#1a1a2e) for headers, zinc grays for body, brand accent for highlights
       - Consistent spacing, margins (72pt page margins), typography scale
       - Reusable style objects: page, header, section, table, footer

    3. Create `src/lib/pdf/components/ReportCover.tsx`:
       - "AKILI Risk Management" branding at top
       - "Family Governance Assessment Report" title
       - Assessment date, completion percentage
       - Overall score prominently displayed with risk level badge text
       - Confidential disclaimer at bottom

    4. Create `src/lib/pdf/components/ExecutiveSummary.tsx`:
       - Props: score (number), riskLevel (string), categoryCount (number), missingControlsCount (number)
       - Narrative paragraph summarizing assessment outcome (dynamic based on risk level)
       - Key metrics in a clean layout: Overall Score, Risk Level, Categories Assessed, Areas of Concern
       - Risk level interpretation text (what LOW/MEDIUM/HIGH/CRITICAL means for their family)

    5. Create `src/lib/pdf/components/CategoryBreakdown.tsx`:
       - Props: breakdown (CategoryScore[])
       - Table with columns: Category Name, Score, Max Score, visual bar representation
       - Color-coded rows based on score thresholds (green >= 7.5, amber >= 5, orange >= 2.5, red < 2.5)
       - Use View with percentage-width backgrounds to simulate progress bars in PDF

    6. Create `src/lib/pdf/components/RecommendationsSection.tsx`:
       - Props: missingControls (MissingControl[])
       - Numbered list of recommendations with severity badge text (High/Medium/Low Priority)
       - Each recommendation shows: category, description, recommendation text
       - "No critical gaps" message when missingControls is empty

    7. Create `src/lib/pdf/components/AssessmentReport.tsx`:
       - Composes all components into a multi-page Document
       - Page 1: ReportCover
       - Page 2: ExecutiveSummary
       - Page 3+: CategoryBreakdown
       - Page 4+: RecommendationsSection
       - Footer on each page: "Confidential - Page X" with generation date
       - All data pre-processed before passing to PDF components (no hooks/state in PDF components per research pitfall #1)

    IMPORTANT: @react-pdf/renderer uses its own renderer, NOT React DOM. No hooks, no browser APIs, no className. Use StyleSheet.create() and style prop only. Import from '@react-pdf/renderer' not 'react'.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no TypeScript errors in PDF components. Verify @react-pdf/renderer is in package.json dependencies.
  </verify>
  <done>
    PDF component library exists with cover, executive summary, category breakdown, and recommendations sections. All components use @react-pdf/renderer primitives with professional styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PDF generation API endpoint</name>
  <files>
    src/app/api/reports/[id]/pdf/route.tsx
  </files>
  <action>
    Create GET endpoint at `/api/reports/[id]/pdf` where `id` is assessmentId:

    1. Auth check: Verify session exists via `auth()`, return 401 if not
    2. Ownership check: Load assessment, verify `userId === session.user.id`, return 403 if not
    3. Score check: Load PillarScore for assessment + "family-governance" pillar, return 404 with message "Complete assessment to generate report" if no score exists
    4. Load user email from session for report header
    5. Load assessment responses count for completion percentage
    6. Pre-process all data into plain objects (no Prisma types in PDF components):
       - score: number, riskLevel: string
       - breakdown: CategoryScore[] (from JSON field)
       - missingControls: MissingControl[] (from JSON field)
       - assessmentDate: formatted string
       - completionPercentage: number
    7. Call `renderToBuffer(<AssessmentReport data={processedData} />)` from '@react-pdf/renderer'
    8. Return NextResponse with buffer, headers:
       - Content-Type: application/pdf
       - Content-Disposition: attachment; filename="belvedere-governance-report.pdf"

    Error handling: Wrap in try/catch, log errors, return 500 with generic message.

    NOTE: This is a .tsx file (not .ts) because it uses JSX for the React PDF component. The route.tsx extension is required for JSX in Next.js API routes.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no TypeScript errors. Verify route file exists at correct path for Next.js dynamic routing.
  </verify>
  <done>
    GET /api/reports/[id]/pdf returns a valid PDF buffer with correct Content-Type and Content-Disposition headers. Auth, ownership, and score existence are validated before generation.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- @react-pdf/renderer in package.json dependencies
- All PDF components import from '@react-pdf/renderer' (not react-dom)
- API route validates auth, ownership, and score existence
- No React hooks or browser APIs used in PDF components
</verification>

<success_criteria>
Server-side PDF generation pipeline is complete: assessment data flows from database through React PDF components to downloadable PDF with executive summary, score breakdown, and recommendations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reports-templates/04-01-SUMMARY.md`
</output>
