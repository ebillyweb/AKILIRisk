---
phase: 23-client-engagement-tracking
plan: "05"
type: execute
wave: 3
depends_on: ["23-03", "23-04"]
files_modified:
  - src/app/(protected)/advisor/engagement/page.tsx
  - src/components/engagement/EngagementDashboard.tsx
  - src/components/engagement/EngagementMetrics.tsx
  - src/components/recommendations/RecommendationsPortfolio.tsx
  - src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx
autonomous: false
requirements: [LIFECYCLE-02]
must_haves:
  truths:
    - "Engagement dashboard page shows completion rates, stalled clients, upcoming milestones, overdue items"
    - "Engagement dashboard page redirects when feature flag is disabled"
    - "Portfolio engagement column shows completion fraction for published clients"
    - "Portfolio engagement column shows -- for unpublished clients"
    - "BLOCKED dot indicator appears in engagement column when any milestone is blocked"
    - "Publish button on advisor guidance page sets actionPlanPublishedAt"
    - "Published badge replaces button after publish"
    - "Advisor who never uses tracking sees zero UI changes"
  artifacts:
    - path: "src/app/(protected)/advisor/engagement/page.tsx"
      provides: "Engagement dashboard SSR page"
      exports: ["default"]
    - path: "src/components/engagement/EngagementDashboard.tsx"
      provides: "Dashboard with metrics, client table, tabs"
      exports: ["EngagementDashboard"]
    - path: "src/components/engagement/EngagementMetrics.tsx"
      provides: "Metric cards for completion, active clients, stalled, overdue"
      exports: ["EngagementMetrics"]
  key_links:
    - from: "src/app/(protected)/advisor/engagement/page.tsx"
      to: "src/lib/engagement/engagement-metrics.ts"
      via: "SSR data fetching"
      pattern: "getEngagementMetrics"
    - from: "src/components/recommendations/RecommendationsPortfolio.tsx"
      to: "src/lib/engagement/engagement-metrics.ts"
      via: "engagement column data"
      pattern: "getPortfolioEngagementData"
---

<objective>
Build the advisor engagement dashboard page, add engagement column to the portfolio, and add the publish action plan button to the advisor guidance page. Final UI surfaces completing Phase 23.

Purpose: Gives advisors portfolio-level visibility into client implementation progress, the ability to drill into stalled clients, and the publish mechanism to activate tracking. Delivers D-07, D-08, D-12 (blocked indicator), and the publish UI (D-02, D-03).

Output: New engagement dashboard page, engagement column in portfolio, publish button on guidance page.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-client-engagement-tracking/23-CONTEXT.md
@.planning/phases/23-client-engagement-tracking/23-UI-SPEC.md
@.planning/phases/23-client-engagement-tracking/23-PATTERNS.md

@.planning/phases/23-client-engagement-tracking/23-03-SUMMARY.md
@.planning/phases/23-client-engagement-tracking/23-04-SUMMARY.md
</context>

<interfaces>
From src/lib/engagement/engagement-metrics.ts (Plan 03):
  export type EngagementMetrics = { overallCompletionPct, activeClientCount, stalledClientCount, overdueMilestoneCount }
  export type EngagementClientRow = { clientId, clientName, clientEmail, completionPct, completedCount, totalCount, lastActivityAt, blockedCount, isStalled }
  export async function getEngagementMetrics(advisorProfileId): Promise<EngagementMetrics>
  export async function getEngagementClients(advisorProfileId, filter?): Promise<EngagementClientRow[]>
  export async function getUpcomingMilestones(advisorProfileId, daysAhead?): Promise<...>
  export async function getPortfolioEngagementData(advisorProfileId): Promise<Map<string, { completedCount, totalCount, blockedCount }>>

From src/lib/engagement/feature-flags.ts (Plan 02):
  export async function isImplementationTrackingEnabled(advisorProfileId): Promise<boolean>

From src/lib/actions/engagement-actions.ts (Plan 03):
  export async function publishActionPlanAction(input): Promise<ActionResult>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create engagement dashboard page and components</name>
  <files>src/app/(protected)/advisor/engagement/page.tsx, src/components/engagement/EngagementDashboard.tsx, src/components/engagement/EngagementMetrics.tsx</files>
  <read_first>
    src/app/(protected)/advisor/analytics/[clientId]/page.tsx (analog -- SSR page with Suspense, requireAdvisorRole, prisma data loading, ArrowLeft back link pattern)
    src/components/action-plan/ProgressDashboard.tsx (Progress component usage pattern)
    .planning/phases/23-client-engagement-tracking/23-UI-SPEC.md (Engagement Dashboard Page section -- layout, metrics, tabs, copywriting, empty state, error state)
    .planning/phases/23-client-engagement-tracking/23-PATTERNS.md (engagement page.tsx section -- SSR page pattern)
    .planning/phases/23-client-engagement-tracking/23-CONTEXT.md (D-08: dashboard page specs)
  </read_first>
  <action>
    1. Create src/app/(protected)/advisor/engagement/page.tsx:
       - Import requireAdvisorRole from @/lib/advisor/auth, redirect from next/navigation, Suspense from react
       - Import isImplementationTrackingEnabled from @/lib/engagement/feature-flags
       - Import EngagementDashboard from @/components/engagement/EngagementDashboard
       - requireAdvisorRole() for auth
       - Find advisorProfile by userId, redirect to /advisor if not found
       - Check isImplementationTrackingEnabled(advisorProfile.id) -- if false, redirect("/advisor/dashboard")
       - Render page shell with:
         - Back link to /advisor/dashboard (ArrowLeft icon pattern)
         - Kicker: "Engagement", Title: "Implementation Tracking", Subtitle: "Track client progress across published action plans"
         - Suspense-wrapped EngagementDashboard component passing advisorProfileId
         - Loading fallback: spinner with "Loading engagement data..."

    2. Create src/components/engagement/EngagementMetrics.tsx:
       - "use client" directive
       - Import Card, CardContent, CardHeader, CardTitle from @/components/ui/card
       - Import Progress from @/components/ui/progress
       - Import type EngagementMetrics from @/lib/engagement/engagement-metrics
       - Export function EngagementMetricsCards({ metrics }: { metrics: EngagementMetrics })
       - 4-card responsive grid: grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-8 (gap xl=32px)
       - Card 1: "Overall Completion" -- metrics.overallCompletionPct as large number + Progress bar
       - Card 2: "Active Clients" -- metrics.activeClientCount
       - Card 3: "Stalled Clients" -- metrics.stalledClientCount (warning color if > 0 via text-chart-5)
       - Card 4: "Overdue Milestones" -- metrics.overdueMilestoneCount (destructive color if > 0 via text-destructive)
       - Each card has aria-label describing the metric value

    3. Create src/components/engagement/EngagementDashboard.tsx:
       - This is a server component (no "use client")
       - Import getEngagementMetrics, getEngagementClients, getUpcomingMilestones from engagement-metrics
       - Import EngagementMetricsCards
       - Import Tabs, TabsContent, TabsList, TabsTrigger from @/components/ui/tabs
       - Import Table, TableBody, TableCell, TableHead, TableHeader, TableRow from @/components/ui/table
       - Import MilestoneStatusBadge from @/components/engagement/MilestoneStatusBadge
       - Import format from date-fns
       - Export async function EngagementDashboard({ advisorProfileId }: { advisorProfileId: string })
       - Fetch metrics, all clients, and upcoming milestones in parallel (Promise.all)
       - If no active clients: render empty state -- "No published action plans" heading + "Publish a client's Strategic Action Plan from their guidance page to begin tracking implementation progress." body text
       - Otherwise render:
         a. EngagementMetricsCards with fetched metrics
         b. Tabs with three tabs:
            - "All Clients": Table with columns Client Name, Completion %, Last Activity, Blocked Count, Actions (link to client guidance page)
            - "Stalled": same table but filtered to stalled clients only, with row highlight using bg-chart-5/5
            - "Upcoming Milestones": list grouped by client showing milestone title, recommendation name, due date, MilestoneStatusBadge
         c. Stalled definition footnote: "No milestone activity in the last 14 days" per copywriting contract
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - /advisor/engagement page redirects when feature flag is disabled
    - Page shows 4 metric cards (Overall Completion, Active Clients, Stalled Clients, Overdue Milestones)
    - Tabs render "All Clients", "Stalled", "Upcoming Milestones"
    - Empty state shows when no published action plans exist
    - Stalled clients highlighted with warning color
    - Each metric card has aria-label
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Engagement dashboard page provides advisors with portfolio-level implementation tracking analytics.</done>
</task>

<task type="auto">
  <name>Task 2: Add engagement column to portfolio and publish button to guidance page</name>
  <files>src/components/recommendations/RecommendationsPortfolio.tsx, src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx</files>
  <read_first>
    src/components/recommendations/RecommendationsPortfolio.tsx (full file -- existing portfolio card component, ClientRecommendationGroup type)
    src/app/(protected)/advisor/clients/[clientId]/guidance/page.tsx (full file -- existing guidance page rendering)
    .planning/phases/23-client-engagement-tracking/23-CONTEXT.md (D-07: engagement column, D-02/D-03: publish mechanism)
    .planning/phases/23-client-engagement-tracking/23-UI-SPEC.md (Engagement Column layout, Publish Action Plan section, Block/Defer Milestone Dialogs)
  </read_first>
  <action>
    1. Extend RecommendationsPortfolio.tsx:
       - Accept new optional prop: engagementData of Map (clientId -> { completedCount, totalCount, blockedCount }) and trackingEnabled boolean
       - When trackingEnabled is false: do not render engagement column (zero noise per D-01)
       - When trackingEnabled is true: add "Engagement" column to each client group card
       - For each client: look up engagementData.get(clientId)
         - If found: render "{completedCount}/{totalCount}" text + circular completion percentage
         - If not found (no published plan): render "--" in text-muted-foreground (D-07)
         - If blockedCount > 0: render 6px red dot next to percentage with aria-label="{N} blocked milestone(s)" and tooltip (D-12)
       - Import Badge from @/components/ui/badge for the blocked indicator

    2. Update the guidance page data loading to pass engagementData and trackingEnabled to RecommendationsPortfolio:
       - In the guidance page SSR component, call isImplementationTrackingEnabled(advisorProfile.id)
       - If enabled, call getPortfolioEngagementData(advisorProfile.id)
       - Pass both to RecommendationsPortfolio

    3. Add publish button to the advisor guidance page (for individual client):
       - Check if the client's assessment has actionPlanPublishedAt set
       - If not published: render "Publish Action Plan" Button (variant="default", primary) in the page header area
         - On click: open confirmation Dialog
         - Dialog title: "Publish action plan?"
         - Dialog body: "{Client Name} will be able to see their Strategic Action Plan and track progress on recommended actions."
         - Confirm: "Publish" button. Cancel: "Not yet" ghost button.
         - On confirm: call publishActionPlanAction server action with the assessmentId
         - On success: revalidate the page (button becomes published badge)
       - If already published: render Badge variant="success" reading "Published {MMM d, yyyy}" (non-interactive) using format from date-fns
       - Create a small client component PublishActionPlanButton for the dialog interaction state

    4. Add Block and Defer milestone dialogs to the guidance page milestone management:
       - When advisor clicks BLOCKED in milestone status dropdown: open Block Milestone Dialog
         - Dialog title: "Block this milestone"
         - Required Textarea: "What is blocking progress?" (min 10 chars, placeholder per copywriting contract)
         - Confirm: "Block Milestone" (destructive variant). Cancel: "Cancel".
         - Calls blockMilestone server action
       - When advisor clicks DEFERRED: open Defer Milestone Dialog
         - Dialog title: "Defer this milestone"
         - Required Textarea: reason + optional Input type="date" for revisit date
         - Confirm: "Defer Milestone" (outline variant). Cancel: "Cancel".
         - Calls deferMilestone server action
       - Create a MilestoneStatusControl client component that wraps the Select dropdown + dialogs
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - RecommendationsPortfolio renders "Engagement" column only when trackingEnabled is true
    - Engagement column shows "--" for clients without published plans
    - Engagement column shows "{completed}/{total}" for clients with published plans
    - Blocked indicator (red dot + aria-label) appears when blockedCount > 0
    - Publish button appears on guidance page when assessment not published
    - Published badge appears when assessment is published
    - Block Milestone Dialog requires reason min 10 characters
    - Defer Milestone Dialog has reason + optional revisit date
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Advisor portfolio shows engagement column with completion data. Guidance page has publish button with confirmation dialog and milestone block/defer dialogs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 23 engagement tracking system:
    1. Milestone BLOCKED/DEFERRED status support with auto-completion
    2. Enterprise feature flag gating all tracking UI
    3. Activity feed on client Strategic Action Plan page
    4. Next Step callout and milestone progress bars
    5. Advisor publish button with confirmation dialog
    6. Engagement dashboard with metrics, client table, stalled/upcoming tabs
    7. Portfolio engagement column with completion percentage and blocked indicators
  </what-built>
  <how-to-verify>
    1. Sign in as advisor (advisor@test.com / Testpass1)
    2. Navigate to a client's guidance page
    3. Verify "Publish Action Plan" button appears in the header area
    4. Click the button and verify confirmation dialog appears
    5. Publish the action plan
    6. Navigate to /advisor/engagement and verify the engagement dashboard loads with metric cards
    7. Navigate back to the portfolio view and verify the "Engagement" column appears
    8. Sign in as client (client@test.com via magic link)
    9. Navigate to the Strategic Action Plan page
    10. Verify milestone progress bars appear below each recommendation
    11. Verify "Next Step" callout card appears above Immediate Priorities
    12. Verify "Recent Activity" collapsible section appears (if activities exist)
    13. Verify milestone checklist is read-only (no dropdowns or buttons)
    14. Sign in as advisor2@test.com (no published plans)
    15. Verify zero tracking UI appears -- no engagement column, no engagement dashboard access
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisor -> engagement dashboard | Advisor sees only their own clients' engagement data |
| advisor -> publish | Must verify advisor-client relationship before publishing |
| client -> action plan | Client sees read-only milestones; no mutation controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | Information Disclosure | EngagementDashboard | mitigate | SSR data fetch uses advisorProfileId from requireAdvisorRole; only own clients visible |
| T-23-10 | Elevation of Privilege | PublishActionPlanButton | mitigate | publishActionPlanAction server action validates advisor role and ownership |
| T-23-11 | Tampering | MilestoneStatusControl | mitigate | blockMilestone/deferMilestone server actions validate advisor role, Zod schema, and ownership |
| T-23-SC | Tampering | npm installs | accept | No new npm packages |
</threat_model>

<verification>
npx tsc --noEmit --project tsconfig.json 2>&1 | head -20
npx vitest run --reporter=verbose 2>&1 | tail -30
npm run build 2>&1 | tail -20
</verification>

<success_criteria>
- Engagement dashboard accessible at /advisor/engagement with feature flag check
- Engagement column visible in portfolio for advisors with tracking enabled
- Publish button works on guidance page with confirmation dialog
- Block/defer milestone dialogs validate input and call server actions
- Zero tracking UI for advisors without published plans
- Build passes
</success_criteria>

<output>
Create `.planning/phases/23-client-engagement-tracking/23-05-SUMMARY.md` when done
</output>
