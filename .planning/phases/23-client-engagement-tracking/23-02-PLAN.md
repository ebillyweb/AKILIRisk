---
phase: 23-client-engagement-tracking
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/engagement/feature-flags.ts
  - src/lib/engagement/activity-feed.ts
  - src/lib/engagement/publish-action-plan.ts
  - src/lib/engagement/feature-flags.test.ts
  - src/lib/engagement/activity-feed.test.ts
autonomous: true
requirements: [LIFECYCLE-02]
must_haves:
  truths:
    - "Feature flag check returns boolean for implementationTrackingEnabled per advisor"
    - "Solo advisors (no enterprise) default to tracking enabled"
    - "Activity feed returns client-scoped activities in reverse-chronological order"
    - "Activity feed supports pagination via cursor-based offset/limit"
    - "Publish action plan sets actionPlanPublishedAt timestamp on Assessment"
  artifacts:
    - path: "src/lib/engagement/feature-flags.ts"
      provides: "isImplementationTrackingEnabled() helper"
      exports: ["isImplementationTrackingEnabled"]
    - path: "src/lib/engagement/activity-feed.ts"
      provides: "getClientActivityFeed() query and ActivityFeedItem type"
      exports: ["getClientActivityFeed", "ActivityFeedItem"]
    - path: "src/lib/engagement/publish-action-plan.ts"
      provides: "publishActionPlan() transactional publish logic"
      exports: ["publishActionPlan"]
  key_links:
    - from: "src/lib/engagement/feature-flags.ts"
      to: "prisma/schema.prisma"
      via: "AdvisorEnterprise.implementationTrackingEnabled query"
      pattern: "implementationTrackingEnabled"
    - from: "src/lib/engagement/activity-feed.ts"
      to: "prisma/schema.prisma"
      via: "SolutionActivity -> AssessmentRecommendation -> Assessment -> userId join"
      pattern: "solutionActivity.findMany"
---

<objective>
Build the three core backend services for Phase 23: enterprise feature flag check, client-scoped activity feed query, and action plan publish mechanism. These are independent utility modules consumed by server actions in Plan 03 and UI components in Plans 04-05.

Purpose: Isolates query logic and business rules into testable service modules separate from server actions (controller layer).

Output: Three service modules with tests in src/lib/engagement/.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-client-engagement-tracking/23-CONTEXT.md
@.planning/phases/23-client-engagement-tracking/23-RESEARCH.md
@.planning/phases/23-client-engagement-tracking/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feature flag helper and activity feed query service</name>
  <files>src/lib/engagement/feature-flags.ts, src/lib/engagement/activity-feed.ts, src/lib/engagement/feature-flags.test.ts, src/lib/engagement/activity-feed.test.ts</files>
  <read_first>
    src/lib/platform/feature-flags.ts (analog -- import pattern, prisma query, safe fallback pattern)
    src/lib/actions/client-action-plan-actions.ts (lines 115-195 -- ActionPlanItem type, Prisma findMany with nested includes, orderBy pattern)
    .planning/phases/23-client-engagement-tracking/23-PATTERNS.md (feature-flags.ts section, activity-feed.ts section)
    .planning/phases/23-client-engagement-tracking/23-CONTEXT.md (D-01, D-04, D-05)
    .planning/phases/23-client-engagement-tracking/23-RESEARCH.md (Pattern 1: Enterprise Feature Flag, Pattern 2: Activity Feed Query)
  </read_first>
  <action>
    1. Create src/lib/engagement/feature-flags.ts:
       - Import "server-only" and prisma from @/lib/db
       - Export async function isImplementationTrackingEnabled(advisorProfileId: string): Promise of boolean
       - Query prisma.advisorProfile.findUnique with where: { id: advisorProfileId }, select: { enterprise: { select: { implementationTrackingEnabled: true } } }
       - Return profile?.enterprise?.implementationTrackingEnabled ?? true (solo advisors default to enabled per RESEARCH.md A4)
       - Add a second export: isTrackingActiveForAssessment(assessmentId: string): Promise of boolean
         - Query prisma.assessment.findUnique to check if actionPlanPublishedAt is not null
         - Returns true only if actionPlanPublishedAt is set (per D-02)

    2. Create src/lib/engagement/activity-feed.ts:
       - Import "server-only" and prisma from @/lib/db
       - Export type ActivityFeedItem with fields: id, action (string), detail (unknown), createdAt (Date), actorId (string | null), recommendationName (string), recommendationId (string)
       - Export async function getClientActivityFeed(input: { clientId: string, limit?: number, offset?: number, role: "ADVISOR" | "CLIENT" }): Promise of ActivityFeedItem[]
       - Query prisma.solutionActivity.findMany with:
         - where: assessmentRecommendation.assessment.userId equals clientId, assessment.status "COMPLETED"
         - include: assessmentRecommendation -> serviceRecommendation -> select name
         - orderBy: createdAt desc
         - take: limit (default 20), skip: offset (default 0)
       - For role "CLIENT": add where filter to exclude advisor-internal action types (actions starting with "status_" that are advisor-only). Include: milestone_update, task_status_update, auto_completed, action_plan_published, validation_requested. Exclude advisor-only actions like status_reviewed, status_declined per D-05.
       - Map results to ActivityFeedItem[] shape

    3. Create test files with vi.mock("@/lib/db") mocking prisma:
       - feature-flags.test.ts: test that enterprise with flag true returns true, flag false returns false, no enterprise (null) returns true (solo advisor default)
       - activity-feed.test.ts: test that query is scoped to clientId, respects limit/offset, CLIENT role excludes advisor-internal actions
  </action>
  <verify>
    <automated>npx vitest run src/lib/engagement/feature-flags.test.ts src/lib/engagement/activity-feed.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/engagement/feature-flags.ts exports isImplementationTrackingEnabled and isTrackingActiveForAssessment
    - isImplementationTrackingEnabled returns true when no enterprise row exists (solo advisor)
    - src/lib/engagement/activity-feed.ts exports getClientActivityFeed and ActivityFeedItem type
    - getClientActivityFeed filters by assessment.userId = clientId
    - CLIENT role filtering excludes advisor-only action types
    - All tests pass
  </acceptance_criteria>
  <done>Feature flag helper returns enterprise-level tracking flag with solo advisor default. Activity feed queries SolutionActivity scoped to a client with role-based filtering and pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Create publish action plan service</name>
  <files>src/lib/engagement/publish-action-plan.ts</files>
  <read_first>
    src/lib/recommendations/solution-lifecycle.ts (lines 96-174 -- transactionRecommendationStatus: $transaction + activity log pattern)
    .planning/phases/23-client-engagement-tracking/23-PATTERNS.md (publish-action-plan.ts section -- transaction + activity log pattern)
    .planning/phases/23-client-engagement-tracking/23-CONTEXT.md (D-02, D-03 -- publish mechanism)
    .planning/phases/23-client-engagement-tracking/23-RESEARCH.md (Pattern 4: Publish Mechanism)
  </read_first>
  <action>
    1. Create src/lib/engagement/publish-action-plan.ts:
       - Import "server-only", prisma from @/lib/db
       - Export async function publishActionPlan(input: { assessmentId: string, actorId: string }): Promise of void
       - Within prisma.$transaction:
         a. Read the assessment: findUniqueOrThrow with where: { id: assessmentId }, select: { actionPlanPublishedAt, status, userId }
         b. Validate: if already published (actionPlanPublishedAt is not null), throw Error("Action plan already published")
         c. Validate: assessment.status must be "COMPLETED" (cannot publish for in-progress assessment)
         d. Update assessment: set actionPlanPublishedAt to new Date()
         e. Find all assessmentRecommendation IDs for this assessment where status is in ["INCLUDED", "IN_PROGRESS", "COMPLETED"] and hiddenFromClient is false
         f. For each recommendation, create a SolutionActivity entry with action "action_plan_published" and detail { assessmentId }
            (Use createMany for efficiency -- single activity per recommendation so the feed shows the event on each recommendation's timeline)

    2. Export a type PublishResult = { publishedAt: Date, recommendationCount: number } and update return type if useful for the UI to show confirmation.

    Follow the exact transactional activity logging pattern from solution-lifecycle.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -c "error" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/engagement/publish-action-plan.ts exports publishActionPlan function
    - Function uses prisma.$transaction for atomic publish + activity logging
    - Validates that actionPlanPublishedAt is null before publishing (idempotency guard)
    - Validates assessment status is COMPLETED
    - Creates SolutionActivity entries with action "action_plan_published"
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Publish mechanism atomically sets actionPlanPublishedAt and logs activity entries for all client-visible recommendations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> activity feed | Client must only see their own activities with role-appropriate filtering |
| advisor -> publish | Advisor must own the client relationship to publish their action plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Information Disclosure | getClientActivityFeed | mitigate | Query filters by assessment.userId = clientId; CLIENT role excludes advisor-internal actions per D-05 |
| T-23-04 | Information Disclosure | publishActionPlan | mitigate | Server action in Plan 03 will verify advisor owns client relationship via requireAdvisorRole + assignment check |
| T-23-SC | Tampering | npm installs | accept | No new npm packages |
</threat_model>

<verification>
npx vitest run src/lib/engagement/ --reporter=verbose
npx tsc --noEmit --project tsconfig.json 2>&1 | head -20
</verification>

<success_criteria>
- Feature flag helper correctly defaults solo advisors to tracking enabled
- Activity feed returns paginated, role-filtered results scoped to one client
- Publish mechanism is transactional with idempotency guard
- All tests pass
- TypeScript compilation clean
</success_criteria>

<output>
Create `.planning/phases/23-client-engagement-tracking/23-02-SUMMARY.md` when done
</output>
