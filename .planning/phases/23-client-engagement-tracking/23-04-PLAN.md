---
phase: 23-client-engagement-tracking
plan: "04"
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - src/components/action-plan/ActivityFeed.tsx
  - src/components/action-plan/NextStepCallout.tsx
  - src/components/action-plan/ProgressDashboard.tsx
  - src/components/action-plan/StrategicActionPlan.tsx
  - src/components/engagement/MilestoneStatusBadge.tsx
  - src/lib/actions/client-action-plan-actions.ts
autonomous: true
requirements: [LIFECYCLE-02]
must_haves:
  truths:
    - "Activity feed renders as collapsible section only when activities exist"
    - "Activity feed groups items by date with reverse-chronological order"
    - "Next Step callout highlights the most urgent incomplete milestone with due date"
    - "ProgressDashboard shows per-recommendation milestone completion percentage"
    - "Client sees milestones as read-only checklist"
    - "All new components return null when no data (zero-state per D-06)"
  artifacts:
    - path: "src/components/action-plan/ActivityFeed.tsx"
      provides: "Collapsible activity feed timeline component"
      exports: ["ActivityFeed"]
    - path: "src/components/action-plan/NextStepCallout.tsx"
      provides: "Next step card with due date badge"
      exports: ["NextStepCallout"]
    - path: "src/components/engagement/MilestoneStatusBadge.tsx"
      provides: "Status badge with BLOCKED/DEFERRED color treatments"
      exports: ["MilestoneStatusBadge"]
  key_links:
    - from: "src/components/action-plan/StrategicActionPlan.tsx"
      to: "src/components/action-plan/ActivityFeed.tsx"
      via: "component composition"
      pattern: "ActivityFeed"
    - from: "src/components/action-plan/StrategicActionPlan.tsx"
      to: "src/components/action-plan/NextStepCallout.tsx"
      via: "component composition"
      pattern: "NextStepCallout"
---

<objective>
Build client-facing UI enhancements: activity feed component, next step callout, milestone progress bars in ProgressDashboard, milestone status badge, and wire them into the Strategic Action Plan page. Extend the data layer to include milestone and activity data.

Purpose: Enhances the client's Strategic Action Plan with engagement tracking surfaces that only appear when tracking is active (published + feature flag). Delivers D-04, D-05, D-06, D-09, D-10.

Output: Four new/enhanced components wired into StrategicActionPlan.tsx. Updated data fetching in client-action-plan-actions.ts.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-client-engagement-tracking/23-CONTEXT.md
@.planning/phases/23-client-engagement-tracking/23-UI-SPEC.md
@.planning/phases/23-client-engagement-tracking/23-PATTERNS.md

@.planning/phases/23-client-engagement-tracking/23-01-SUMMARY.md
@.planning/phases/23-client-engagement-tracking/23-02-SUMMARY.md
</context>

<interfaces>
From src/components/action-plan/StrategicActionPlan.tsx:
  export function StrategicActionPlan({ data }: { data: ClientActionPlanData })
  // Composes ExecutiveSummary, TimeHorizonSections, ProgressDashboard

From src/components/action-plan/ProgressDashboard.tsx:
  export function ProgressDashboard({ items }: { items: ActionPlanItem[] })
  // Returns null when items.length === 0

From src/lib/actions/client-action-plan-actions.ts:
  export type ActionPlanItem = { id, name, description, category, taskStatus, playbookSteps, ... }
  export type ClientActionPlanData = { immediate, strategic, ongoing }

From src/lib/engagement/activity-feed.ts (Plan 02):
  export type ActivityFeedItem = { id, action, detail, createdAt, actorId, recommendationName, recommendationId }
  export async function getClientActivityFeed(input): Promise<ActivityFeedItem[]>

From src/lib/engagement/feature-flags.ts (Plan 02):
  export async function isImplementationTrackingEnabled(advisorProfileId: string): Promise<boolean>
  export async function isTrackingActiveForAssessment(assessmentId: string): Promise<boolean>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MilestoneStatusBadge, ActivityFeed, and NextStepCallout components</name>
  <files>src/components/engagement/MilestoneStatusBadge.tsx, src/components/action-plan/ActivityFeed.tsx, src/components/action-plan/NextStepCallout.tsx</files>
  <read_first>
    src/components/pipeline/WorkflowTimeline.tsx (lines 40-94 -- timeline dot + connector + content pattern)
    src/components/action-plan/ActionCard.tsx (lines 221-258 -- Collapsible import and usage pattern)
    src/components/action-plan/ProgressDashboard.tsx (full file -- zero-state return null pattern, Progress component usage)
    src/components/recommendations/RecommendationsPortfolio.tsx (lines 9-23 -- statusBadgeVariant pattern)
    .planning/phases/23-client-engagement-tracking/23-UI-SPEC.md (Activity Feed layout, Next Step Callout layout, Milestone Status Badge Colors, Copywriting Contract, Accessibility Requirements)
    .planning/phases/23-client-engagement-tracking/23-PATTERNS.md (ActivityFeed.tsx, NextStepCallout.tsx, MilestoneStatusBadge.tsx sections)
  </read_first>
  <action>
    1. Create src/components/engagement/MilestoneStatusBadge.tsx:
       - "use client" directive
       - Import Badge from @/components/ui/badge
       - Export function MilestoneStatusBadge({ status }: { status: string })
       - Map status to Badge variant + className per UI-SPEC:
         NOT_STARTED -> variant="outline"
         IN_PROGRESS -> className "bg-brand/10 text-brand-foreground border-brand/20"
         COMPLETED -> className "bg-chart-2/15 text-foreground border-chart-2/30"
         SKIPPED -> variant="secondary"
         BLOCKED -> className "bg-destructive/10 text-destructive border-destructive/20"
         DEFERRED -> className "bg-chart-5/15 text-foreground border-chart-5/30"
       - Display status.toLowerCase().replace("_", " ") as label
       - Badge className includes "h-5 text-[10px]" per existing portfolio pattern

    2. Create src/components/action-plan/ActivityFeed.tsx:
       - "use client" directive
       - Import Collapsible, CollapsibleContent, CollapsibleTrigger from @/components/ui/collapsible
       - Import format, isSameDay, subDays from date-fns
       - Import ChevronDown from lucide-react
       - Import useState for collapsible state and "show more" pagination
       - Define type ActivityFeedProps = { activities: ActivityFeedItem[], onLoadMore?: () => void, hasMore?: boolean }
       - Export function ActivityFeed: return null when activities.length === 0 (D-06 zero-state)
       - Wrap in Collapsible (default collapsed)
       - Trigger text: "Recent Activity ({activities.length})" per copywriting contract
       - Group activities by date: "Today", "Yesterday", "MMM d, yyyy" using date-fns
       - Each date group: sticky header with text-xs text-muted-foreground font-semibold on bg-muted/50
       - Each activity row: 24px dot column (8px colored circle + 2px connector) + content column
       - Content: action description (text-sm), recommendation name (text-xs text-muted-foreground), relative timestamp (text-xs, right-aligned)
       - Action descriptions per copywriting: "Milestone {status}: {title}", "{Name} updated {Rec} to {status}", auto_completed -> "{Rec} automatically marked as completed"
       - "Show more activity" ghost button at bottom when hasMore is true
       - role="feed" aria-label="Recent activity" on feed container, role="article" on each item per accessibility reqs

    3. Create src/components/action-plan/NextStepCallout.tsx:
       - "use client" directive
       - Import Card from @/components/ui/card, Badge from @/components/ui/badge
       - Import AlertCircle from lucide-react, format from date-fns
       - Define type MilestoneInfo = { title: string, dueDate: Date | null, recommendationName: string }
       - Export function NextStepCallout({ milestones }: { milestones: MilestoneInfo[] })
       - Find the next step: filter to non-terminal milestones, prefer one with earliest dueDate, fallback to first by sort order
       - Return null if no next step found (all milestones terminal)
       - Card with 3px left border using border-l-[3px] border-l-brand
       - Interior: AlertCircle icon (20px) + "Next Step" heading (text-sm font-semibold) + milestone title (text-sm) + due date badge (variant="outline", shows "Due {MMM d}" or "No due date")
       - role="status" aria-live="polite" on the card per accessibility reqs
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MilestoneStatusBadge renders all 6 statuses with correct color treatments from UI-SPEC
    - ActivityFeed returns null when activities is empty (D-06)
    - ActivityFeed uses Collapsible with trigger text "Recent Activity ({count})"
    - ActivityFeed has role="feed" and aria-label="Recent activity"
    - NextStepCallout returns null when all milestones are terminal
    - NextStepCallout has Card with 3px left border using brand color
    - NextStepCallout has role="status" aria-live="polite"
    - All three components compile without TypeScript errors
  </acceptance_criteria>
  <done>Three new engagement UI components created following UI-SPEC design contract and accessibility requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Extend data layer and wire components into StrategicActionPlan</name>
  <files>src/lib/actions/client-action-plan-actions.ts, src/components/action-plan/ProgressDashboard.tsx, src/components/action-plan/StrategicActionPlan.tsx</files>
  <read_first>
    src/lib/actions/client-action-plan-actions.ts (full file -- ActionPlanItem type, getClientActionPlan query, PlaybookStep type)
    src/components/action-plan/StrategicActionPlan.tsx (full file -- current composition of sections)
    src/components/action-plan/ProgressDashboard.tsx (full file -- current progress calculation)
    src/app/(protected)/dashboard/action-plan/page.tsx (how StrategicActionPlan is rendered -- need to pass activity feed data)
    .planning/phases/23-client-engagement-tracking/23-CONTEXT.md (D-06, D-09, D-10)
    .planning/phases/23-client-engagement-tracking/23-UI-SPEC.md (Milestone Progress Bars layout, interaction contracts)
  </read_first>
  <action>
    1. Extend ActionPlanItem type in client-action-plan-actions.ts:
       - Add milestones field: array of { id: string, title: string, status: string, dueDate: Date | null, completedAt: Date | null }
       - Add milestoneCompletionPct: number (computed)

    2. Update getClientActionPlan() query in client-action-plan-actions.ts:
       - Add milestones to the include on assessmentRecommendation: { select: { id, title, status, dueDate, completedAt }, orderBy: { sortOrder: "asc" } }
       - Compute milestoneCompletionPct per recommendation: (milestones with terminal status) / (total milestones) * 100
       - Add a new export: getActionPlanTrackingContext() that returns { isTrackingActive: boolean, activities: ActivityFeedItem[], allMilestones: MilestoneInfo[] }
         - This function checks isTrackingActiveForAssessment (from feature-flags) and loads activity feed data if active
         - If tracking is not active, returns { isTrackingActive: false, activities: [], allMilestones: [] }
         - Import getClientActivityFeed from @/lib/engagement/activity-feed and isTrackingActiveForAssessment from @/lib/engagement/feature-flags

    3. Extend ProgressDashboard.tsx:
       - Accept optional milestones data on ActionPlanItem
       - Below each per-recommendation progress bar, add milestone completion Progress bar (h-1.5, fill uses brand color)
       - Label: "{completed}/{total} milestones" in text-xs text-muted-foreground
       - Below the milestone progress bar, add a Collapsible with read-only milestone checklist
         - Each row: MilestoneStatusBadge + milestone title (text-sm)
         - Client cannot interact (D-10 read-only). No dropdowns, no buttons.
       - Only render milestone section when item.milestones and item.milestones.length > 0
       - Progress bar must have role="progressbar" with aria-valuenow, aria-valuemin=0, aria-valuemax=100, aria-label="{name} milestone progress"

    4. Update StrategicActionPlan.tsx:
       - Accept new optional prop: trackingContext of { isTrackingActive: boolean, activities: ActivityFeedItem[], allMilestones: MilestoneInfo[] }
       - When isTrackingActive is true:
         a. Render NextStepCallout above the first TimeHorizonSection (per D-09 position)
         b. Render ActivityFeed after ProgressDashboard (per D-06 inline section)
       - When isTrackingActive is false: render exactly as today (zero change)

    5. Update the action plan page (src/app/(protected)/dashboard/action-plan/page.tsx):
       - Call getActionPlanTrackingContext() alongside getClientActionPlan()
       - Pass trackingContext to StrategicActionPlan
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ActionPlanItem type includes milestones array and milestoneCompletionPct
    - getClientActionPlan returns milestones data per recommendation
    - getActionPlanTrackingContext returns isTrackingActive false when assessment not published
    - ProgressDashboard renders milestone progress bars with role="progressbar" and aria attributes
    - StrategicActionPlan renders NextStepCallout and ActivityFeed only when isTrackingActive is true
    - StrategicActionPlan renders exactly as before when isTrackingActive is false (zero noise per D-06)
    - action-plan/page.tsx passes trackingContext to StrategicActionPlan
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Client Action Plan page shows milestone progress bars, read-only milestone checklists, next step callout, and activity feed -- all gated behind publish + feature flag for zero noise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> action plan | Client sees only their own data; milestone checklist is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Information Disclosure | getActionPlanTrackingContext | mitigate | Uses existing getClientActivityFeed which filters by session userId |
| T-23-08 | Tampering | ProgressDashboard milestones | accept | Read-only display; no mutation actions exposed to client in this component |
| T-23-SC | Tampering | npm installs | accept | No new npm packages |
</threat_model>

<verification>
npx tsc --noEmit --project tsconfig.json 2>&1 | head -20
npx vitest run --reporter=verbose 2>&1 | tail -20
</verification>

<success_criteria>
- Client with published action plan sees milestone progress bars and next step callout
- Client without published action plan sees exactly today's UI (zero noise)
- Activity feed renders only when activities exist
- Milestone checklist is read-only (no interactive controls for client)
- TypeScript compilation clean
</success_criteria>

<output>
Create `.planning/phases/23-client-engagement-tracking/23-04-SUMMARY.md` when done
</output>
