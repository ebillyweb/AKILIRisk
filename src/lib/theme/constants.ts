/** localStorage key for explicit light/dark choice (system follows when unset). */
export const AKILIRISK_THEME_STORAGE_KEY = "akilirisk-theme" as const;

export type AkiliriskStoredTheme = "light" | "dark";
