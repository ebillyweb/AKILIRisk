/** Visible scored questions in pillar DDL for deploy health checks. */
export function deploymentVisibleQuestionCount(input: {
  pillarVisibleCount: number;
}): number {
  return input.pillarVisibleCount;
}
