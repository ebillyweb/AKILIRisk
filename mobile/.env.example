# Base URL of the AkiliRisk web API (NextAuth + REST endpoints).
# For local web dev against `npm run dev`, use your machine's LAN IP so a
# physical device / simulator can reach it, e.g. http://192.168.1.20:3000
EXPO_PUBLIC_API_BASE_URL=https://app.akilirisk.com
